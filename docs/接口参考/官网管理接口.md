# 官网管理接口

官网管理（Website）提供通用官网 CMS 数据能力。本期后台数据管理已提供插件页面，面向官网访客展示的前台页面不在本期实现；官网展示与第三方读取统一调用 `/website/**` 开放 API。后台接口需要 System 登录态；开放 API 必须使用 `appid + appkey + HMAC-SHA256` 签名，并按接口应用绑定的 `site_id + tenant_id` 过滤公开数据。

## 接口总览

后台菜单由 `plugin/Website/plugin.json` 提供，入口为 `/system/website`，包含“官网站点、接口应用、官网栏目、官网导航、官网内容、官网区块、访客线索”七个管理页。面向普通访客的 `dmsai.cn` 官网页面不在后台实现，前端或第三方服务端需调用带签名的 `/website/**` 开放接口自行渲染。

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/system/website/site/index` | 站点列表 |
| GET | `/system/website/site/info/{id}` | 站点详情 |
| GET | `/system/website/site/options` | 站点选项 |
| POST | `/system/website/site/create` | 新增站点 |
| PUT | `/system/website/site/update/{id}` | 编辑站点 |
| PUT | `/system/website/site/status/{id}` | 更新站点状态 |
| DELETE | `/system/website/site/delete/{ids}` | 删除站点 |
| PUT | `/system/website/site/recovery/{ids}` | 恢复站点 |
| DELETE | `/system/website/site/real-delete/{ids}` | 彻底删除站点 |
| GET | `/system/website/app/index` | 接口应用列表 |
| GET | `/system/website/app/info/{id}` | 接口应用详情 |
| GET | `/system/website/app/scope-options` | 接口权限选项 |
| POST | `/system/website/app/create` | 新增接口应用 |
| PUT | `/system/website/app/update/{id}` | 编辑接口应用 |
| PUT | `/system/website/app/reset-key/{id}` | 重置接口应用密钥 |
| PUT | `/system/website/app/status/{id}` | 更新接口应用状态 |
| DELETE | `/system/website/app/delete/{ids}` | 删除接口应用 |
| PUT | `/system/website/app/recovery/{ids}` | 恢复接口应用 |
| DELETE | `/system/website/app/real-delete/{ids}` | 彻底删除接口应用 |
| GET | `/system/website/channel/index` | 栏目列表 |
| GET | `/system/website/channel/tree` | 栏目树 |
| GET | `/system/website/channel/info/{id}` | 栏目详情 |
| GET | `/system/website/channel/options` | 栏目选项 |
| POST | `/system/website/channel/create` | 新增栏目 |
| PUT | `/system/website/channel/update/{id}` | 编辑栏目 |
| PUT | `/system/website/channel/status/{id}` | 更新栏目状态 |
| DELETE | `/system/website/channel/delete/{ids}` | 删除栏目 |
| PUT | `/system/website/channel/recovery/{ids}` | 恢复栏目 |
| DELETE | `/system/website/channel/real-delete/{ids}` | 彻底删除栏目 |
| GET | `/system/website/nav/index` | 导航列表 |
| GET | `/system/website/nav/tree` | 导航树 |
| GET | `/system/website/nav/info/{id}` | 导航详情 |
| GET | `/system/website/nav/options` | 导航选项 |
| POST | `/system/website/nav/create` | 新增导航 |
| PUT | `/system/website/nav/update/{id}` | 编辑导航 |
| PUT | `/system/website/nav/status/{id}` | 更新导航状态 |
| DELETE | `/system/website/nav/delete/{ids}` | 删除导航 |
| PUT | `/system/website/nav/recovery/{ids}` | 恢复导航 |
| DELETE | `/system/website/nav/real-delete/{ids}` | 彻底删除导航 |
| GET | `/system/website/content/index` | 内容列表 |
| GET | `/system/website/content/info/{id}` | 内容详情 |
| GET | `/system/website/content/options` | 内容选项 |
| POST | `/system/website/content/create` | 新增内容 |
| PUT | `/system/website/content/update/{id}` | 编辑内容 |
| PUT | `/system/website/content/publish/{id}` | 发布内容 |
| PUT | `/system/website/content/offline/{id}` | 下线内容 |
| PUT | `/system/website/content/status/{id}` | 更新内容状态 |
| DELETE | `/system/website/content/delete/{ids}` | 删除内容 |
| PUT | `/system/website/content/recovery/{ids}` | 恢复内容 |
| DELETE | `/system/website/content/real-delete/{ids}` | 彻底删除内容 |
| GET | `/system/website/block/index` | 区块列表 |
| GET | `/system/website/block/info/{id}` | 区块详情 |
| GET | `/system/website/block/options` | 区块选项 |
| POST | `/system/website/block/create` | 新增区块 |
| PUT | `/system/website/block/update/{id}` | 编辑区块 |
| PUT | `/system/website/block/status/{id}` | 更新区块状态 |
| DELETE | `/system/website/block/delete/{ids}` | 删除区块 |
| PUT | `/system/website/block/recovery/{ids}` | 恢复区块 |
| DELETE | `/system/website/block/real-delete/{ids}` | 彻底删除区块 |
| GET | `/system/website/lead/index` | 线索列表 |
| GET | `/system/website/lead/info/{id}` | 线索详情 |
| PUT | `/system/website/lead/handle/{id}` | 处理线索 |
| PUT | `/system/website/lead/status/{id}` | 更新线索状态 |
| DELETE | `/system/website/lead/delete/{ids}` | 删除线索 |
| GET | `/website/bootstrap` | 开放初始化，需签名 |
| GET | `/website/site` | 开放站点，需签名 |
| GET | `/website/nav/tree` | 开放导航树，需签名 |
| GET | `/website/channel/tree` | 开放栏目树，需签名 |
| GET | `/website/page` | 开放页面，需签名 |
| GET | `/website/content/index` | 开放内容列表，需签名 |
| GET | `/website/content/info/{id}` | 开放内容详情，需签名 |
| GET | `/website/content/slug/{slug}` | 开放 Slug 内容，需签名 |
| GET | `/website/block/list` | 开放区块列表，需签名 |
| POST | `/website/lead/create` | 开放线索提交，需签名 |

## 开放 API 签名规则

`/website/**` 只面向第三方服务端或可信服务端代理调用。调用方先在后台“官网管理 → 接口应用”创建应用，保存一次性返回的 `AppID` 与 `AppKey`。

所有开放 API 请求必须携带以下请求头：

| Header | 说明 |
|--------|------|
| `X-Website-Appid` | 接口应用 AppID |
| `X-Website-Timestamp` | Unix 秒级时间戳，允许与服务端相差 300 秒 |
| `X-Website-Nonce` | 8-64 位随机串，同一应用 300 秒内不可重复 |
| `X-Website-Sign` | HMAC-SHA256 小写 hex 签名 |

签名串：

```text
METHOD + "\n" + PATH + "\n" + canonicalQuery + "\n" + sha256(rawBody) + "\n" + timestamp + "\n" + nonce
```

签名算法：

```text
sign = hash_hmac("sha256", stringToSign, appKey)
```

`canonicalQuery` 按参数名升序排序后使用 RFC3986 编码，POST JSON 请求体使用原始 body 计算 SHA256。验签通过后，服务端只使用接口应用绑定的站点；`site` 参数可以传应用绑定站点的 `code`、主域名或别名，但不能切换到其他站点。

接口权限按应用的 `scopes` 控制：`bootstrap` 会组合读取站点、导航、栏目和首页区块，需要 `site:read`、`nav:read`、`channel:read`、`block:read`；`page` 会组合读取站点、栏目/内容命中结果和页面区块，需要 `page:read`、`site:read`、`channel:read`、`content:read`、`block:read`；内容列表/详情、区块列表、线索提交分别使用 `content:read`、`block:read`、`lead:create`。

PHP 示例：

```php
$method = 'GET';
$path = '/website/content/index';
$query = ['channel' => 'news', 'type' => 'news', 'page' => 1, 'pageSize' => 10];
ksort($query);
$rawBody = '';
$timestamp = (string)time();
$nonce = bin2hex(random_bytes(8));
$canonicalQuery = http_build_query($query, '', '&', PHP_QUERY_RFC3986);
$stringToSign = strtoupper($method) . "\n" . $path . "\n" . $canonicalQuery . "\n" . hash('sha256', $rawBody) . "\n" . $timestamp . "\n" . $nonce;
$sign = hash_hmac('sha256', $stringToSign, $appKey);
```

签名缺失、过期、重复或错误返回 body.code `401`；应用禁用、IP 不允许、scope 不允许或超限返回 body.code `403`；HTTP status 仍保持 `200`。

## 请求案例

<!-- API_CASES_START -->

### 站点列表

<!-- API_CASE: GET /system/website/site/index -->
```jsonc
{
  // method: 请求方法
  "method": "GET",
  // path: 请求路径，路径参数在案例中替换为可调试值
  "path": "/system/website/site/index",
  // headers: 后台接口填写 Authorization，公开接口可为空对象
  "headers": {
    "Authorization": "Bearer ${TOKEN}"
  },
  // query: URL 查询参数
  "query": {
    "page": 1,
    "pageSize": 10,
    "keyword": ""
  },
  // body: 请求体，GET/DELETE 通常为空对象
  "body": {}
}
```
```api-test
{
  "method": "GET",
  "path": "/system/website/site/index",
  "headers": {
    "Authorization": "Bearer ${TOKEN}"
  },
  "query": {
    "page": 1,
    "pageSize": 10,
    "keyword": ""
  },
  "body": {}
}
```

### 站点详情

<!-- API_CASE: GET /system/website/site/info/{id} -->
```jsonc
{
  // method: 请求方法
  "method": "GET",
  // path: 请求路径，路径参数在案例中替换为可调试值
  "path": "/system/website/site/info/1",
  // headers: 后台接口填写 Authorization，公开接口可为空对象
  "headers": {
    "Authorization": "Bearer ${TOKEN}"
  },
  // query: URL 查询参数
  "query": {},
  // body: 请求体，GET/DELETE 通常为空对象
  "body": {}
}
```
```api-test
{
  "method": "GET",
  "path": "/system/website/site/info/1",
  "headers": {
    "Authorization": "Bearer ${TOKEN}"
  },
  "query": {},
  "body": {}
}
```

### 站点选项

<!-- API_CASE: GET /system/website/site/options -->
```jsonc
{
  // method: 请求方法
  "method": "GET",
  // path: 请求路径，路径参数在案例中替换为可调试值
  "path": "/system/website/site/options",
  // headers: 后台接口填写 Authorization，公开接口可为空对象
  "headers": {
    "Authorization": "Bearer ${TOKEN}"
  },
  // query: URL 查询参数
  "query": {
    "site_id": 1
  },
  // body: 请求体，GET/DELETE 通常为空对象
  "body": {}
}
```
```api-test
{
  "method": "GET",
  "path": "/system/website/site/options",
  "headers": {
    "Authorization": "Bearer ${TOKEN}"
  },
  "query": {
    "site_id": 1
  },
  "body": {}
}
```

### 新增站点

<!-- API_CASE: POST /system/website/site/create -->
```jsonc
{
  // method: 请求方法
  "method": "POST",
  // path: 请求路径，路径参数在案例中替换为可调试值
  "path": "/system/website/site/create",
  // headers: 后台接口填写 Authorization，公开接口可为空对象
  "headers": {
    "Authorization": "Bearer ${TOKEN}"
  },
  // query: URL 查询参数
  "query": {},
  // body: 请求体，GET/DELETE 通常为空对象
  "body": {
    "code": "dmsai",
    "name": "德玛仕智慧厨房",
    "domain": "dmsai.cn",
    "aliases": [
      "www.dmsai.cn"
    ],
    "seo": {
      "title": "德玛仕智慧厨房"
    },
    "contact": {
      "phone": "400-863-7077"
    },
    "config": {},
    "status": 1
  }
}
```
```api-test
{
  "method": "POST",
  "path": "/system/website/site/create",
  "headers": {
    "Authorization": "Bearer ${TOKEN}"
  },
  "query": {},
  "body": {
    "code": "dmsai",
    "name": "德玛仕智慧厨房",
    "domain": "dmsai.cn",
    "aliases": [
      "www.dmsai.cn"
    ],
    "seo": {
      "title": "德玛仕智慧厨房"
    },
    "contact": {
      "phone": "400-863-7077"
    },
    "config": {},
    "status": 1
  }
}
```

### 编辑站点

<!-- API_CASE: PUT /system/website/site/update/{id} -->
```jsonc
{
  // method: 请求方法
  "method": "PUT",
  // path: 请求路径，路径参数在案例中替换为可调试值
  "path": "/system/website/site/update/1",
  // headers: 后台接口填写 Authorization，公开接口可为空对象
  "headers": {
    "Authorization": "Bearer ${TOKEN}"
  },
  // query: URL 查询参数
  "query": {},
  // body: 请求体，GET/DELETE 通常为空对象
  "body": {
    "code": "dmsai",
    "name": "德玛仕智慧厨房",
    "domain": "dmsai.cn",
    "aliases": [
      "www.dmsai.cn"
    ],
    "seo": {
      "title": "德玛仕智慧厨房"
    },
    "contact": {
      "phone": "400-863-7077"
    },
    "config": {},
    "status": 1
  }
}
```
```api-test
{
  "method": "PUT",
  "path": "/system/website/site/update/1",
  "headers": {
    "Authorization": "Bearer ${TOKEN}"
  },
  "query": {},
  "body": {
    "code": "dmsai",
    "name": "德玛仕智慧厨房",
    "domain": "dmsai.cn",
    "aliases": [
      "www.dmsai.cn"
    ],
    "seo": {
      "title": "德玛仕智慧厨房"
    },
    "contact": {
      "phone": "400-863-7077"
    },
    "config": {},
    "status": 1
  }
}
```

### 更新站点状态

<!-- API_CASE: PUT /system/website/site/status/{id} -->
```jsonc
{
  // method: 请求方法
  "method": "PUT",
  // path: 请求路径，路径参数在案例中替换为可调试值
  "path": "/system/website/site/status/1",
  // headers: 后台接口填写 Authorization，公开接口可为空对象
  "headers": {
    "Authorization": "Bearer ${TOKEN}"
  },
  // query: URL 查询参数
  "query": {},
  // body: 请求体，GET/DELETE 通常为空对象
  "body": {
    "status": 1
  }
}
```
```api-test
{
  "method": "PUT",
  "path": "/system/website/site/status/1",
  "headers": {
    "Authorization": "Bearer ${TOKEN}"
  },
  "query": {},
  "body": {
    "status": 1
  }
}
```

### 删除站点

<!-- API_CASE: DELETE /system/website/site/delete/{ids} -->
```jsonc
{
  // method: 请求方法
  "method": "DELETE",
  // path: 请求路径，路径参数在案例中替换为可调试值
  "path": "/system/website/site/delete/1,2",
  // headers: 后台接口填写 Authorization，公开接口可为空对象
  "headers": {
    "Authorization": "Bearer ${TOKEN}"
  },
  // query: URL 查询参数
  "query": {},
  // body: 请求体，GET/DELETE 通常为空对象
  "body": {}
}
```
```api-test
{
  "method": "DELETE",
  "path": "/system/website/site/delete/1,2",
  "headers": {
    "Authorization": "Bearer ${TOKEN}"
  },
  "query": {},
  "body": {}
}
```

### 恢复站点

<!-- API_CASE: PUT /system/website/site/recovery/{ids} -->
```jsonc
{
  // method: 请求方法
  "method": "PUT",
  // path: 请求路径，路径参数在案例中替换为可调试值
  "path": "/system/website/site/recovery/1,2",
  // headers: 后台接口填写 Authorization，公开接口可为空对象
  "headers": {
    "Authorization": "Bearer ${TOKEN}"
  },
  // query: URL 查询参数
  "query": {},
  // body: 请求体，GET/DELETE 通常为空对象
  "body": {}
}
```
```api-test
{
  "method": "PUT",
  "path": "/system/website/site/recovery/1,2",
  "headers": {
    "Authorization": "Bearer ${TOKEN}"
  },
  "query": {},
  "body": {}
}
```

### 彻底删除站点

<!-- API_CASE: DELETE /system/website/site/real-delete/{ids} -->
```jsonc
{
  // method: 请求方法
  "method": "DELETE",
  // path: 请求路径，路径参数在案例中替换为可调试值
  "path": "/system/website/site/real-delete/1,2",
  // headers: 后台接口填写 Authorization，公开接口可为空对象
  "headers": {
    "Authorization": "Bearer ${TOKEN}"
  },
  // query: URL 查询参数
  "query": {},
  // body: 请求体，GET/DELETE 通常为空对象
  "body": {}
}
```
```api-test
{
  "method": "DELETE",
  "path": "/system/website/site/real-delete/1,2",
  "headers": {
    "Authorization": "Bearer ${TOKEN}"
  },
  "query": {},
  "body": {}
}
```


### 接口应用列表

<!-- API_CASE: GET /system/website/app/index -->
```jsonc
{
  // method: 请求方法
  "method": "GET",
  // path: 请求路径，路径参数在案例中替换为可调试值
  "path": "/system/website/app/index",
  // headers: 后台接口填写 Authorization，开放接口填写 X-Website-* 签名头
  "headers": {
    "Authorization": "Bearer ${TOKEN}"
  },
  // query: URL 查询参数
  "query": {
    "page": 1,
    "pageSize": 10,
    "keyword": "",
    "site_id": 1,
    "status": 1
  },
  // body: 请求体，GET/DELETE 通常为空对象
  "body": {}
}
```
```api-test
{
  "method": "GET",
  "path": "/system/website/app/index",
  "headers": {
    "Authorization": "Bearer ${TOKEN}"
  },
  "query": {
    "page": 1,
    "pageSize": 10,
    "keyword": "",
    "site_id": 1,
    "status": 1
  },
  "body": {}
}
```

### 接口应用详情

<!-- API_CASE: GET /system/website/app/info/{id} -->
```jsonc
{
  // method: 请求方法
  "method": "GET",
  // path: 请求路径，路径参数在案例中替换为可调试值
  "path": "/system/website/app/info/1",
  // headers: 后台接口填写 Authorization，开放接口填写 X-Website-* 签名头
  "headers": {
    "Authorization": "Bearer ${TOKEN}"
  },
  // query: URL 查询参数
  "query": {},
  // body: 请求体，GET/DELETE 通常为空对象
  "body": {}
}
```
```api-test
{
  "method": "GET",
  "path": "/system/website/app/info/1",
  "headers": {
    "Authorization": "Bearer ${TOKEN}"
  },
  "query": {},
  "body": {}
}
```

### 接口权限选项

<!-- API_CASE: GET /system/website/app/scope-options -->
```jsonc
{
  // method: 请求方法
  "method": "GET",
  // path: 请求路径，路径参数在案例中替换为可调试值
  "path": "/system/website/app/scope-options",
  // headers: 后台接口填写 Authorization，开放接口填写 X-Website-* 签名头
  "headers": {
    "Authorization": "Bearer ${TOKEN}"
  },
  // query: URL 查询参数
  "query": {},
  // body: 请求体，GET/DELETE 通常为空对象
  "body": {}
}
```
```api-test
{
  "method": "GET",
  "path": "/system/website/app/scope-options",
  "headers": {
    "Authorization": "Bearer ${TOKEN}"
  },
  "query": {},
  "body": {}
}
```

### 新增接口应用

<!-- API_CASE: POST /system/website/app/create -->
```jsonc
{
  // method: 请求方法
  "method": "POST",
  // path: 请求路径，路径参数在案例中替换为可调试值
  "path": "/system/website/app/create",
  // headers: 后台接口填写 Authorization，开放接口填写 X-Website-* 签名头
  "headers": {
    "Authorization": "Bearer ${TOKEN}"
  },
  // query: URL 查询参数
  "query": {},
  // body: 请求体，GET/DELETE 通常为空对象
  "body": {
    "site_id": 1,
    "name": "官网前端服务端",
    "scopes": ["site:read", "nav:read", "channel:read", "page:read", "content:read", "block:read"],
    "ip_whitelist": ["203.0.113.10"],
    "rate_limit": 60,
    "status": 1,
    "remark": "AppKey 明文只在创建响应返回一次"
  }
}
```
```api-test
{
  "method": "POST",
  "path": "/system/website/app/create",
  "headers": {
    "Authorization": "Bearer ${TOKEN}"
  },
  "query": {},
  "body": {
    "site_id": 1,
    "name": "官网前端服务端",
    "scopes": ["site:read", "nav:read", "channel:read", "page:read", "content:read", "block:read"],
    "ip_whitelist": ["203.0.113.10"],
    "rate_limit": 60,
    "status": 1,
    "remark": "AppKey 明文只在创建响应返回一次"
  }
}
```

### 编辑接口应用

<!-- API_CASE: PUT /system/website/app/update/{id} -->
```jsonc
{
  // method: 请求方法
  "method": "PUT",
  // path: 请求路径，路径参数在案例中替换为可调试值
  "path": "/system/website/app/update/1",
  // headers: 后台接口填写 Authorization，开放接口填写 X-Website-* 签名头
  "headers": {
    "Authorization": "Bearer ${TOKEN}"
  },
  // query: URL 查询参数
  "query": {},
  // body: 请求体，GET/DELETE 通常为空对象
  "body": {
    "site_id": 1,
    "name": "官网前端服务端",
    "scopes": ["site:read", "nav:read", "channel:read", "page:read", "content:read", "block:read", "lead:create"],
    "ip_whitelist": [],
    "rate_limit": 120,
    "status": 1,
    "remark": "允许提交线索"
  }
}
```
```api-test
{
  "method": "PUT",
  "path": "/system/website/app/update/1",
  "headers": {
    "Authorization": "Bearer ${TOKEN}"
  },
  "query": {},
  "body": {
    "site_id": 1,
    "name": "官网前端服务端",
    "scopes": ["site:read", "nav:read", "channel:read", "page:read", "content:read", "block:read", "lead:create"],
    "ip_whitelist": [],
    "rate_limit": 120,
    "status": 1,
    "remark": "允许提交线索"
  }
}
```

### 重置接口应用密钥

<!-- API_CASE: PUT /system/website/app/reset-key/{id} -->
```jsonc
{
  // method: 请求方法
  "method": "PUT",
  // path: 请求路径，路径参数在案例中替换为可调试值
  "path": "/system/website/app/reset-key/1",
  // headers: 后台接口填写 Authorization，开放接口填写 X-Website-* 签名头
  "headers": {
    "Authorization": "Bearer ${TOKEN}"
  },
  // query: URL 查询参数
  "query": {},
  // body: 请求体，GET/DELETE 通常为空对象
  "body": {}
}
```
```api-test
{
  "method": "PUT",
  "path": "/system/website/app/reset-key/1",
  "headers": {
    "Authorization": "Bearer ${TOKEN}"
  },
  "query": {},
  "body": {}
}
```

### 更新接口应用状态

<!-- API_CASE: PUT /system/website/app/status/{id} -->
```jsonc
{
  // method: 请求方法
  "method": "PUT",
  // path: 请求路径，路径参数在案例中替换为可调试值
  "path": "/system/website/app/status/1",
  // headers: 后台接口填写 Authorization，开放接口填写 X-Website-* 签名头
  "headers": {
    "Authorization": "Bearer ${TOKEN}"
  },
  // query: URL 查询参数
  "query": {},
  // body: 请求体，GET/DELETE 通常为空对象
  "body": {
    "status": 0
  }
}
```
```api-test
{
  "method": "PUT",
  "path": "/system/website/app/status/1",
  "headers": {
    "Authorization": "Bearer ${TOKEN}"
  },
  "query": {},
  "body": {
    "status": 0
  }
}
```

### 删除接口应用

<!-- API_CASE: DELETE /system/website/app/delete/{ids} -->
```jsonc
{
  // method: 请求方法
  "method": "DELETE",
  // path: 请求路径，路径参数在案例中替换为可调试值
  "path": "/system/website/app/delete/1,2",
  // headers: 后台接口填写 Authorization，开放接口填写 X-Website-* 签名头
  "headers": {
    "Authorization": "Bearer ${TOKEN}"
  },
  // query: URL 查询参数
  "query": {},
  // body: 请求体，GET/DELETE 通常为空对象
  "body": {}
}
```
```api-test
{
  "method": "DELETE",
  "path": "/system/website/app/delete/1,2",
  "headers": {
    "Authorization": "Bearer ${TOKEN}"
  },
  "query": {},
  "body": {}
}
```

### 恢复接口应用

<!-- API_CASE: PUT /system/website/app/recovery/{ids} -->
```jsonc
{
  // method: 请求方法
  "method": "PUT",
  // path: 请求路径，路径参数在案例中替换为可调试值
  "path": "/system/website/app/recovery/1,2",
  // headers: 后台接口填写 Authorization，开放接口填写 X-Website-* 签名头
  "headers": {
    "Authorization": "Bearer ${TOKEN}"
  },
  // query: URL 查询参数
  "query": {},
  // body: 请求体，GET/DELETE 通常为空对象
  "body": {}
}
```
```api-test
{
  "method": "PUT",
  "path": "/system/website/app/recovery/1,2",
  "headers": {
    "Authorization": "Bearer ${TOKEN}"
  },
  "query": {},
  "body": {}
}
```

### 彻底删除接口应用

<!-- API_CASE: DELETE /system/website/app/real-delete/{ids} -->
```jsonc
{
  // method: 请求方法
  "method": "DELETE",
  // path: 请求路径，路径参数在案例中替换为可调试值
  "path": "/system/website/app/real-delete/1,2",
  // headers: 后台接口填写 Authorization，开放接口填写 X-Website-* 签名头
  "headers": {
    "Authorization": "Bearer ${TOKEN}"
  },
  // query: URL 查询参数
  "query": {},
  // body: 请求体，GET/DELETE 通常为空对象
  "body": {}
}
```
```api-test
{
  "method": "DELETE",
  "path": "/system/website/app/real-delete/1,2",
  "headers": {
    "Authorization": "Bearer ${TOKEN}"
  },
  "query": {},
  "body": {}
}
```


### 栏目列表

<!-- API_CASE: GET /system/website/channel/index -->
```jsonc
{
  // method: 请求方法
  "method": "GET",
  // path: 请求路径，路径参数在案例中替换为可调试值
  "path": "/system/website/channel/index",
  // headers: 后台接口填写 Authorization，公开接口可为空对象
  "headers": {
    "Authorization": "Bearer ${TOKEN}"
  },
  // query: URL 查询参数
  "query": {
    "page": 1,
    "pageSize": 10,
    "keyword": ""
  },
  // body: 请求体，GET/DELETE 通常为空对象
  "body": {}
}
```
```api-test
{
  "method": "GET",
  "path": "/system/website/channel/index",
  "headers": {
    "Authorization": "Bearer ${TOKEN}"
  },
  "query": {
    "page": 1,
    "pageSize": 10,
    "keyword": ""
  },
  "body": {}
}
```

### 栏目树

<!-- API_CASE: GET /system/website/channel/tree -->
```jsonc
{
  // method: 请求方法
  "method": "GET",
  // path: 请求路径，路径参数在案例中替换为可调试值
  "path": "/system/website/channel/tree",
  // headers: 后台接口填写 Authorization，公开接口可为空对象
  "headers": {
    "Authorization": "Bearer ${TOKEN}"
  },
  // query: URL 查询参数
  "query": {
    "site_id": 1
  },
  // body: 请求体，GET/DELETE 通常为空对象
  "body": {}
}
```
```api-test
{
  "method": "GET",
  "path": "/system/website/channel/tree",
  "headers": {
    "Authorization": "Bearer ${TOKEN}"
  },
  "query": {
    "site_id": 1
  },
  "body": {}
}
```

### 栏目详情

<!-- API_CASE: GET /system/website/channel/info/{id} -->
```jsonc
{
  // method: 请求方法
  "method": "GET",
  // path: 请求路径，路径参数在案例中替换为可调试值
  "path": "/system/website/channel/info/1",
  // headers: 后台接口填写 Authorization，公开接口可为空对象
  "headers": {
    "Authorization": "Bearer ${TOKEN}"
  },
  // query: URL 查询参数
  "query": {},
  // body: 请求体，GET/DELETE 通常为空对象
  "body": {}
}
```
```api-test
{
  "method": "GET",
  "path": "/system/website/channel/info/1",
  "headers": {
    "Authorization": "Bearer ${TOKEN}"
  },
  "query": {},
  "body": {}
}
```

### 栏目选项

<!-- API_CASE: GET /system/website/channel/options -->
```jsonc
{
  // method: 请求方法
  "method": "GET",
  // path: 请求路径，路径参数在案例中替换为可调试值
  "path": "/system/website/channel/options",
  // headers: 后台接口填写 Authorization，公开接口可为空对象
  "headers": {
    "Authorization": "Bearer ${TOKEN}"
  },
  // query: URL 查询参数
  "query": {
    "site_id": 1
  },
  // body: 请求体，GET/DELETE 通常为空对象
  "body": {}
}
```
```api-test
{
  "method": "GET",
  "path": "/system/website/channel/options",
  "headers": {
    "Authorization": "Bearer ${TOKEN}"
  },
  "query": {
    "site_id": 1
  },
  "body": {}
}
```

### 新增栏目

<!-- API_CASE: POST /system/website/channel/create -->
```jsonc
{
  // method: 请求方法
  "method": "POST",
  // path: 请求路径，路径参数在案例中替换为可调试值
  "path": "/system/website/channel/create",
  // headers: 后台接口填写 Authorization，公开接口可为空对象
  "headers": {
    "Authorization": "Bearer ${TOKEN}"
  },
  // query: URL 查询参数
  "query": {},
  // body: 请求体，GET/DELETE 通常为空对象
  "body": {
    "site_id": 1,
    "parent_id": 0,
    "code": "safe",
    "name": "智慧食安",
    "route": "/safe/",
    "type": "page",
    "seo": {
      "title": "智慧食安"
    },
    "sort": 80,
    "status": 1
  }
}
```
```api-test
{
  "method": "POST",
  "path": "/system/website/channel/create",
  "headers": {
    "Authorization": "Bearer ${TOKEN}"
  },
  "query": {},
  "body": {
    "site_id": 1,
    "parent_id": 0,
    "code": "safe",
    "name": "智慧食安",
    "route": "/safe/",
    "type": "page",
    "seo": {
      "title": "智慧食安"
    },
    "sort": 80,
    "status": 1
  }
}
```

### 编辑栏目

<!-- API_CASE: PUT /system/website/channel/update/{id} -->
```jsonc
{
  // method: 请求方法
  "method": "PUT",
  // path: 请求路径，路径参数在案例中替换为可调试值
  "path": "/system/website/channel/update/1",
  // headers: 后台接口填写 Authorization，公开接口可为空对象
  "headers": {
    "Authorization": "Bearer ${TOKEN}"
  },
  // query: URL 查询参数
  "query": {},
  // body: 请求体，GET/DELETE 通常为空对象
  "body": {
    "site_id": 1,
    "parent_id": 0,
    "code": "safe",
    "name": "智慧食安",
    "route": "/safe/",
    "type": "page",
    "seo": {
      "title": "智慧食安"
    },
    "sort": 80,
    "status": 1
  }
}
```
```api-test
{
  "method": "PUT",
  "path": "/system/website/channel/update/1",
  "headers": {
    "Authorization": "Bearer ${TOKEN}"
  },
  "query": {},
  "body": {
    "site_id": 1,
    "parent_id": 0,
    "code": "safe",
    "name": "智慧食安",
    "route": "/safe/",
    "type": "page",
    "seo": {
      "title": "智慧食安"
    },
    "sort": 80,
    "status": 1
  }
}
```

### 更新栏目状态

<!-- API_CASE: PUT /system/website/channel/status/{id} -->
```jsonc
{
  // method: 请求方法
  "method": "PUT",
  // path: 请求路径，路径参数在案例中替换为可调试值
  "path": "/system/website/channel/status/1",
  // headers: 后台接口填写 Authorization，公开接口可为空对象
  "headers": {
    "Authorization": "Bearer ${TOKEN}"
  },
  // query: URL 查询参数
  "query": {},
  // body: 请求体，GET/DELETE 通常为空对象
  "body": {
    "status": 1
  }
}
```
```api-test
{
  "method": "PUT",
  "path": "/system/website/channel/status/1",
  "headers": {
    "Authorization": "Bearer ${TOKEN}"
  },
  "query": {},
  "body": {
    "status": 1
  }
}
```

### 删除栏目

<!-- API_CASE: DELETE /system/website/channel/delete/{ids} -->
```jsonc
{
  // method: 请求方法
  "method": "DELETE",
  // path: 请求路径，路径参数在案例中替换为可调试值
  "path": "/system/website/channel/delete/1,2",
  // headers: 后台接口填写 Authorization，公开接口可为空对象
  "headers": {
    "Authorization": "Bearer ${TOKEN}"
  },
  // query: URL 查询参数
  "query": {},
  // body: 请求体，GET/DELETE 通常为空对象
  "body": {}
}
```
```api-test
{
  "method": "DELETE",
  "path": "/system/website/channel/delete/1,2",
  "headers": {
    "Authorization": "Bearer ${TOKEN}"
  },
  "query": {},
  "body": {}
}
```

### 恢复栏目

<!-- API_CASE: PUT /system/website/channel/recovery/{ids} -->
```jsonc
{
  // method: 请求方法
  "method": "PUT",
  // path: 请求路径，路径参数在案例中替换为可调试值
  "path": "/system/website/channel/recovery/1,2",
  // headers: 后台接口填写 Authorization，公开接口可为空对象
  "headers": {
    "Authorization": "Bearer ${TOKEN}"
  },
  // query: URL 查询参数
  "query": {},
  // body: 请求体，GET/DELETE 通常为空对象
  "body": {}
}
```
```api-test
{
  "method": "PUT",
  "path": "/system/website/channel/recovery/1,2",
  "headers": {
    "Authorization": "Bearer ${TOKEN}"
  },
  "query": {},
  "body": {}
}
```

### 彻底删除栏目

<!-- API_CASE: DELETE /system/website/channel/real-delete/{ids} -->
```jsonc
{
  // method: 请求方法
  "method": "DELETE",
  // path: 请求路径，路径参数在案例中替换为可调试值
  "path": "/system/website/channel/real-delete/1,2",
  // headers: 后台接口填写 Authorization，公开接口可为空对象
  "headers": {
    "Authorization": "Bearer ${TOKEN}"
  },
  // query: URL 查询参数
  "query": {},
  // body: 请求体，GET/DELETE 通常为空对象
  "body": {}
}
```
```api-test
{
  "method": "DELETE",
  "path": "/system/website/channel/real-delete/1,2",
  "headers": {
    "Authorization": "Bearer ${TOKEN}"
  },
  "query": {},
  "body": {}
}
```

### 导航列表

<!-- API_CASE: GET /system/website/nav/index -->
```jsonc
{
  // method: 请求方法
  "method": "GET",
  // path: 请求路径，路径参数在案例中替换为可调试值
  "path": "/system/website/nav/index",
  // headers: 后台接口填写 Authorization，公开接口可为空对象
  "headers": {
    "Authorization": "Bearer ${TOKEN}"
  },
  // query: URL 查询参数
  "query": {
    "page": 1,
    "pageSize": 10,
    "keyword": ""
  },
  // body: 请求体，GET/DELETE 通常为空对象
  "body": {}
}
```
```api-test
{
  "method": "GET",
  "path": "/system/website/nav/index",
  "headers": {
    "Authorization": "Bearer ${TOKEN}"
  },
  "query": {
    "page": 1,
    "pageSize": 10,
    "keyword": ""
  },
  "body": {}
}
```

### 导航树

<!-- API_CASE: GET /system/website/nav/tree -->
```jsonc
{
  // method: 请求方法
  "method": "GET",
  // path: 请求路径，路径参数在案例中替换为可调试值
  "path": "/system/website/nav/tree",
  // headers: 后台接口填写 Authorization，公开接口可为空对象
  "headers": {
    "Authorization": "Bearer ${TOKEN}"
  },
  // query: URL 查询参数
  "query": {
    "site_id": 1
  },
  // body: 请求体，GET/DELETE 通常为空对象
  "body": {}
}
```
```api-test
{
  "method": "GET",
  "path": "/system/website/nav/tree",
  "headers": {
    "Authorization": "Bearer ${TOKEN}"
  },
  "query": {
    "site_id": 1
  },
  "body": {}
}
```

### 导航详情

<!-- API_CASE: GET /system/website/nav/info/{id} -->
```jsonc
{
  // method: 请求方法
  "method": "GET",
  // path: 请求路径，路径参数在案例中替换为可调试值
  "path": "/system/website/nav/info/1",
  // headers: 后台接口填写 Authorization，公开接口可为空对象
  "headers": {
    "Authorization": "Bearer ${TOKEN}"
  },
  // query: URL 查询参数
  "query": {},
  // body: 请求体，GET/DELETE 通常为空对象
  "body": {}
}
```
```api-test
{
  "method": "GET",
  "path": "/system/website/nav/info/1",
  "headers": {
    "Authorization": "Bearer ${TOKEN}"
  },
  "query": {},
  "body": {}
}
```

### 导航选项

<!-- API_CASE: GET /system/website/nav/options -->
```jsonc
{
  // method: 请求方法
  "method": "GET",
  // path: 请求路径，路径参数在案例中替换为可调试值
  "path": "/system/website/nav/options",
  // headers: 后台接口填写 Authorization，公开接口可为空对象
  "headers": {
    "Authorization": "Bearer ${TOKEN}"
  },
  // query: URL 查询参数
  "query": {
    "site_id": 1
  },
  // body: 请求体，GET/DELETE 通常为空对象
  "body": {}
}
```
```api-test
{
  "method": "GET",
  "path": "/system/website/nav/options",
  "headers": {
    "Authorization": "Bearer ${TOKEN}"
  },
  "query": {
    "site_id": 1
  },
  "body": {}
}
```

### 新增导航

<!-- API_CASE: POST /system/website/nav/create -->
```jsonc
{
  // method: 请求方法
  "method": "POST",
  // path: 请求路径，路径参数在案例中替换为可调试值
  "path": "/system/website/nav/create",
  // headers: 后台接口填写 Authorization，公开接口可为空对象
  "headers": {
    "Authorization": "Bearer ${TOKEN}"
  },
  // query: URL 查询参数
  "query": {},
  // body: 请求体，GET/DELETE 通常为空对象
  "body": {
    "site_id": 1,
    "parent_id": 0,
    "position": "top",
    "title": "智慧食安",
    "link_type": "route",
    "route": "/safe/",
    "target": "self",
    "sort": 80,
    "status": 1
  }
}
```
```api-test
{
  "method": "POST",
  "path": "/system/website/nav/create",
  "headers": {
    "Authorization": "Bearer ${TOKEN}"
  },
  "query": {},
  "body": {
    "site_id": 1,
    "parent_id": 0,
    "position": "top",
    "title": "智慧食安",
    "link_type": "route",
    "route": "/safe/",
    "target": "self",
    "sort": 80,
    "status": 1
  }
}
```

### 编辑导航

<!-- API_CASE: PUT /system/website/nav/update/{id} -->
```jsonc
{
  // method: 请求方法
  "method": "PUT",
  // path: 请求路径，路径参数在案例中替换为可调试值
  "path": "/system/website/nav/update/1",
  // headers: 后台接口填写 Authorization，公开接口可为空对象
  "headers": {
    "Authorization": "Bearer ${TOKEN}"
  },
  // query: URL 查询参数
  "query": {},
  // body: 请求体，GET/DELETE 通常为空对象
  "body": {
    "site_id": 1,
    "parent_id": 0,
    "position": "top",
    "title": "智慧食安",
    "link_type": "route",
    "route": "/safe/",
    "target": "self",
    "sort": 80,
    "status": 1
  }
}
```
```api-test
{
  "method": "PUT",
  "path": "/system/website/nav/update/1",
  "headers": {
    "Authorization": "Bearer ${TOKEN}"
  },
  "query": {},
  "body": {
    "site_id": 1,
    "parent_id": 0,
    "position": "top",
    "title": "智慧食安",
    "link_type": "route",
    "route": "/safe/",
    "target": "self",
    "sort": 80,
    "status": 1
  }
}
```

### 更新导航状态

<!-- API_CASE: PUT /system/website/nav/status/{id} -->
```jsonc
{
  // method: 请求方法
  "method": "PUT",
  // path: 请求路径，路径参数在案例中替换为可调试值
  "path": "/system/website/nav/status/1",
  // headers: 后台接口填写 Authorization，公开接口可为空对象
  "headers": {
    "Authorization": "Bearer ${TOKEN}"
  },
  // query: URL 查询参数
  "query": {},
  // body: 请求体，GET/DELETE 通常为空对象
  "body": {
    "status": 1
  }
}
```
```api-test
{
  "method": "PUT",
  "path": "/system/website/nav/status/1",
  "headers": {
    "Authorization": "Bearer ${TOKEN}"
  },
  "query": {},
  "body": {
    "status": 1
  }
}
```

### 删除导航

<!-- API_CASE: DELETE /system/website/nav/delete/{ids} -->
```jsonc
{
  // method: 请求方法
  "method": "DELETE",
  // path: 请求路径，路径参数在案例中替换为可调试值
  "path": "/system/website/nav/delete/1,2",
  // headers: 后台接口填写 Authorization，公开接口可为空对象
  "headers": {
    "Authorization": "Bearer ${TOKEN}"
  },
  // query: URL 查询参数
  "query": {},
  // body: 请求体，GET/DELETE 通常为空对象
  "body": {}
}
```
```api-test
{
  "method": "DELETE",
  "path": "/system/website/nav/delete/1,2",
  "headers": {
    "Authorization": "Bearer ${TOKEN}"
  },
  "query": {},
  "body": {}
}
```

### 恢复导航

<!-- API_CASE: PUT /system/website/nav/recovery/{ids} -->
```jsonc
{
  // method: 请求方法
  "method": "PUT",
  // path: 请求路径，路径参数在案例中替换为可调试值
  "path": "/system/website/nav/recovery/1,2",
  // headers: 后台接口填写 Authorization，公开接口可为空对象
  "headers": {
    "Authorization": "Bearer ${TOKEN}"
  },
  // query: URL 查询参数
  "query": {},
  // body: 请求体，GET/DELETE 通常为空对象
  "body": {}
}
```
```api-test
{
  "method": "PUT",
  "path": "/system/website/nav/recovery/1,2",
  "headers": {
    "Authorization": "Bearer ${TOKEN}"
  },
  "query": {},
  "body": {}
}
```

### 彻底删除导航

<!-- API_CASE: DELETE /system/website/nav/real-delete/{ids} -->
```jsonc
{
  // method: 请求方法
  "method": "DELETE",
  // path: 请求路径，路径参数在案例中替换为可调试值
  "path": "/system/website/nav/real-delete/1,2",
  // headers: 后台接口填写 Authorization，公开接口可为空对象
  "headers": {
    "Authorization": "Bearer ${TOKEN}"
  },
  // query: URL 查询参数
  "query": {},
  // body: 请求体，GET/DELETE 通常为空对象
  "body": {}
}
```
```api-test
{
  "method": "DELETE",
  "path": "/system/website/nav/real-delete/1,2",
  "headers": {
    "Authorization": "Bearer ${TOKEN}"
  },
  "query": {},
  "body": {}
}
```

### 内容列表

<!-- API_CASE: GET /system/website/content/index -->
```jsonc
{
  // method: 请求方法
  "method": "GET",
  // path: 请求路径，路径参数在案例中替换为可调试值
  "path": "/system/website/content/index",
  // headers: 后台接口填写 Authorization，公开接口可为空对象
  "headers": {
    "Authorization": "Bearer ${TOKEN}"
  },
  // query: URL 查询参数
  "query": {
    "page": 1,
    "pageSize": 10,
    "keyword": ""
  },
  // body: 请求体，GET/DELETE 通常为空对象
  "body": {}
}
```
```api-test
{
  "method": "GET",
  "path": "/system/website/content/index",
  "headers": {
    "Authorization": "Bearer ${TOKEN}"
  },
  "query": {
    "page": 1,
    "pageSize": 10,
    "keyword": ""
  },
  "body": {}
}
```

### 内容详情

<!-- API_CASE: GET /system/website/content/info/{id} -->
```jsonc
{
  // method: 请求方法
  "method": "GET",
  // path: 请求路径，路径参数在案例中替换为可调试值
  "path": "/system/website/content/info/1",
  // headers: 后台接口填写 Authorization，公开接口可为空对象
  "headers": {
    "Authorization": "Bearer ${TOKEN}"
  },
  // query: URL 查询参数
  "query": {},
  // body: 请求体，GET/DELETE 通常为空对象
  "body": {}
}
```
```api-test
{
  "method": "GET",
  "path": "/system/website/content/info/1",
  "headers": {
    "Authorization": "Bearer ${TOKEN}"
  },
  "query": {},
  "body": {}
}
```

### 内容选项

<!-- API_CASE: GET /system/website/content/options -->
```jsonc
{
  // method: 请求方法
  "method": "GET",
  // path: 请求路径，路径参数在案例中替换为可调试值
  "path": "/system/website/content/options",
  // headers: 后台接口填写 Authorization，公开接口可为空对象
  "headers": {
    "Authorization": "Bearer ${TOKEN}"
  },
  // query: URL 查询参数
  "query": {
    "site_id": 1
  },
  // body: 请求体，GET/DELETE 通常为空对象
  "body": {}
}
```
```api-test
{
  "method": "GET",
  "path": "/system/website/content/options",
  "headers": {
    "Authorization": "Bearer ${TOKEN}"
  },
  "query": {
    "site_id": 1
  },
  "body": {}
}
```

### 新增内容

<!-- API_CASE: POST /system/website/content/create -->
```jsonc
{
  // method: 请求方法
  "method": "POST",
  // path: 请求路径，路径参数在案例中替换为可调试值
  "path": "/system/website/content/create",
  // headers: 后台接口填写 Authorization，公开接口可为空对象
  "headers": {
    "Authorization": "Bearer ${TOKEN}"
  },
  // query: URL 查询参数
  "query": {},
  // body: 请求体，GET/DELETE 通常为空对象
  "body": {
    "site_id": 1,
    "channel_id": 1,
    "type": "news",
    "title": "官网内容标题",
    "slug": "demo-news",
    "route": "/news/demo-news/",
    "summary": "内容摘要",
    "cover": "",
    "content_html": "<p>正文</p>",
    "payload": {},
    "tags": [
      "官网"
    ],
    "seo": {
      "title": "官网内容标题"
    },
    "sort": 0,
    "is_top": 0,
    "publish_status": "draft",
    "status": 1
  }
}
```
```api-test
{
  "method": "POST",
  "path": "/system/website/content/create",
  "headers": {
    "Authorization": "Bearer ${TOKEN}"
  },
  "query": {},
  "body": {
    "site_id": 1,
    "channel_id": 1,
    "type": "news",
    "title": "官网内容标题",
    "slug": "demo-news",
    "route": "/news/demo-news/",
    "summary": "内容摘要",
    "cover": "",
    "content_html": "<p>正文</p>",
    "payload": {},
    "tags": [
      "官网"
    ],
    "seo": {
      "title": "官网内容标题"
    },
    "sort": 0,
    "is_top": 0,
    "publish_status": "draft",
    "status": 1
  }
}
```

### 编辑内容

<!-- API_CASE: PUT /system/website/content/update/{id} -->
```jsonc
{
  // method: 请求方法
  "method": "PUT",
  // path: 请求路径，路径参数在案例中替换为可调试值
  "path": "/system/website/content/update/1",
  // headers: 后台接口填写 Authorization，公开接口可为空对象
  "headers": {
    "Authorization": "Bearer ${TOKEN}"
  },
  // query: URL 查询参数
  "query": {},
  // body: 请求体，GET/DELETE 通常为空对象
  "body": {
    "site_id": 1,
    "channel_id": 1,
    "type": "news",
    "title": "官网内容标题",
    "slug": "demo-news",
    "route": "/news/demo-news/",
    "summary": "内容摘要",
    "cover": "",
    "content_html": "<p>正文</p>",
    "payload": {},
    "tags": [
      "官网"
    ],
    "seo": {
      "title": "官网内容标题"
    },
    "sort": 0,
    "is_top": 0,
    "publish_status": "draft",
    "status": 1
  }
}
```
```api-test
{
  "method": "PUT",
  "path": "/system/website/content/update/1",
  "headers": {
    "Authorization": "Bearer ${TOKEN}"
  },
  "query": {},
  "body": {
    "site_id": 1,
    "channel_id": 1,
    "type": "news",
    "title": "官网内容标题",
    "slug": "demo-news",
    "route": "/news/demo-news/",
    "summary": "内容摘要",
    "cover": "",
    "content_html": "<p>正文</p>",
    "payload": {},
    "tags": [
      "官网"
    ],
    "seo": {
      "title": "官网内容标题"
    },
    "sort": 0,
    "is_top": 0,
    "publish_status": "draft",
    "status": 1
  }
}
```

### 发布内容

<!-- API_CASE: PUT /system/website/content/publish/{id} -->
```jsonc
{
  // method: 请求方法
  "method": "PUT",
  // path: 请求路径，路径参数在案例中替换为可调试值
  "path": "/system/website/content/publish/1",
  // headers: 后台接口填写 Authorization，公开接口可为空对象
  "headers": {
    "Authorization": "Bearer ${TOKEN}"
  },
  // query: URL 查询参数
  "query": {},
  // body: 请求体，GET/DELETE 通常为空对象
  "body": {
    "published_at": "2026-05-28 10:00:00",
    "offline_at": ""
  }
}
```
```api-test
{
  "method": "PUT",
  "path": "/system/website/content/publish/1",
  "headers": {
    "Authorization": "Bearer ${TOKEN}"
  },
  "query": {},
  "body": {
    "published_at": "2026-05-28 10:00:00",
    "offline_at": ""
  }
}
```

### 下线内容

<!-- API_CASE: PUT /system/website/content/offline/{id} -->
```jsonc
{
  // method: 请求方法
  "method": "PUT",
  // path: 请求路径，路径参数在案例中替换为可调试值
  "path": "/system/website/content/offline/1",
  // headers: 后台接口填写 Authorization，公开接口可为空对象
  "headers": {
    "Authorization": "Bearer ${TOKEN}"
  },
  // query: URL 查询参数
  "query": {},
  // body: 请求体，GET/DELETE 通常为空对象
  "body": {}
}
```
```api-test
{
  "method": "PUT",
  "path": "/system/website/content/offline/1",
  "headers": {
    "Authorization": "Bearer ${TOKEN}"
  },
  "query": {},
  "body": {}
}
```

### 更新内容状态

<!-- API_CASE: PUT /system/website/content/status/{id} -->
```jsonc
{
  // method: 请求方法
  "method": "PUT",
  // path: 请求路径，路径参数在案例中替换为可调试值
  "path": "/system/website/content/status/1",
  // headers: 后台接口填写 Authorization，公开接口可为空对象
  "headers": {
    "Authorization": "Bearer ${TOKEN}"
  },
  // query: URL 查询参数
  "query": {},
  // body: 请求体，GET/DELETE 通常为空对象
  "body": {
    "status": 1
  }
}
```
```api-test
{
  "method": "PUT",
  "path": "/system/website/content/status/1",
  "headers": {
    "Authorization": "Bearer ${TOKEN}"
  },
  "query": {},
  "body": {
    "status": 1
  }
}
```

### 删除内容

<!-- API_CASE: DELETE /system/website/content/delete/{ids} -->
```jsonc
{
  // method: 请求方法
  "method": "DELETE",
  // path: 请求路径，路径参数在案例中替换为可调试值
  "path": "/system/website/content/delete/1,2",
  // headers: 后台接口填写 Authorization，公开接口可为空对象
  "headers": {
    "Authorization": "Bearer ${TOKEN}"
  },
  // query: URL 查询参数
  "query": {},
  // body: 请求体，GET/DELETE 通常为空对象
  "body": {}
}
```
```api-test
{
  "method": "DELETE",
  "path": "/system/website/content/delete/1,2",
  "headers": {
    "Authorization": "Bearer ${TOKEN}"
  },
  "query": {},
  "body": {}
}
```

### 恢复内容

<!-- API_CASE: PUT /system/website/content/recovery/{ids} -->
```jsonc
{
  // method: 请求方法
  "method": "PUT",
  // path: 请求路径，路径参数在案例中替换为可调试值
  "path": "/system/website/content/recovery/1,2",
  // headers: 后台接口填写 Authorization，公开接口可为空对象
  "headers": {
    "Authorization": "Bearer ${TOKEN}"
  },
  // query: URL 查询参数
  "query": {},
  // body: 请求体，GET/DELETE 通常为空对象
  "body": {}
}
```
```api-test
{
  "method": "PUT",
  "path": "/system/website/content/recovery/1,2",
  "headers": {
    "Authorization": "Bearer ${TOKEN}"
  },
  "query": {},
  "body": {}
}
```

### 彻底删除内容

<!-- API_CASE: DELETE /system/website/content/real-delete/{ids} -->
```jsonc
{
  // method: 请求方法
  "method": "DELETE",
  // path: 请求路径，路径参数在案例中替换为可调试值
  "path": "/system/website/content/real-delete/1,2",
  // headers: 后台接口填写 Authorization，公开接口可为空对象
  "headers": {
    "Authorization": "Bearer ${TOKEN}"
  },
  // query: URL 查询参数
  "query": {},
  // body: 请求体，GET/DELETE 通常为空对象
  "body": {}
}
```
```api-test
{
  "method": "DELETE",
  "path": "/system/website/content/real-delete/1,2",
  "headers": {
    "Authorization": "Bearer ${TOKEN}"
  },
  "query": {},
  "body": {}
}
```

### 区块列表

<!-- API_CASE: GET /system/website/block/index -->
```jsonc
{
  // method: 请求方法
  "method": "GET",
  // path: 请求路径，路径参数在案例中替换为可调试值
  "path": "/system/website/block/index",
  // headers: 后台接口填写 Authorization，公开接口可为空对象
  "headers": {
    "Authorization": "Bearer ${TOKEN}"
  },
  // query: URL 查询参数
  "query": {
    "page": 1,
    "pageSize": 10,
    "keyword": ""
  },
  // body: 请求体，GET/DELETE 通常为空对象
  "body": {}
}
```
```api-test
{
  "method": "GET",
  "path": "/system/website/block/index",
  "headers": {
    "Authorization": "Bearer ${TOKEN}"
  },
  "query": {
    "page": 1,
    "pageSize": 10,
    "keyword": ""
  },
  "body": {}
}
```

### 区块详情

<!-- API_CASE: GET /system/website/block/info/{id} -->
```jsonc
{
  // method: 请求方法
  "method": "GET",
  // path: 请求路径，路径参数在案例中替换为可调试值
  "path": "/system/website/block/info/1",
  // headers: 后台接口填写 Authorization，公开接口可为空对象
  "headers": {
    "Authorization": "Bearer ${TOKEN}"
  },
  // query: URL 查询参数
  "query": {},
  // body: 请求体，GET/DELETE 通常为空对象
  "body": {}
}
```
```api-test
{
  "method": "GET",
  "path": "/system/website/block/info/1",
  "headers": {
    "Authorization": "Bearer ${TOKEN}"
  },
  "query": {},
  "body": {}
}
```

### 区块选项

<!-- API_CASE: GET /system/website/block/options -->
```jsonc
{
  // method: 请求方法
  "method": "GET",
  // path: 请求路径，路径参数在案例中替换为可调试值
  "path": "/system/website/block/options",
  // headers: 后台接口填写 Authorization，公开接口可为空对象
  "headers": {
    "Authorization": "Bearer ${TOKEN}"
  },
  // query: URL 查询参数
  "query": {
    "site_id": 1
  },
  // body: 请求体，GET/DELETE 通常为空对象
  "body": {}
}
```
```api-test
{
  "method": "GET",
  "path": "/system/website/block/options",
  "headers": {
    "Authorization": "Bearer ${TOKEN}"
  },
  "query": {
    "site_id": 1
  },
  "body": {}
}
```

### 新增区块

<!-- API_CASE: POST /system/website/block/create -->
```jsonc
{
  // method: 请求方法
  "method": "POST",
  // path: 请求路径，路径参数在案例中替换为可调试值
  "path": "/system/website/block/create",
  // headers: 后台接口填写 Authorization，公开接口可为空对象
  "headers": {
    "Authorization": "Bearer ${TOKEN}"
  },
  // query: URL 查询参数
  "query": {},
  // body: 请求体，GET/DELETE 通常为空对象
  "body": {
    "site_id": 1,
    "page_code": "home",
    "group_code": "hero",
    "code": "main-hero",
    "name": "首页首屏",
    "type": "hero",
    "title": "德玛仕智慧厨房",
    "subtitle": "整体智慧厨房一站式解决方案服务商",
    "payload": {},
    "media": {},
    "link": {},
    "sort": 100,
    "publish_status": "draft",
    "status": 1
  }
}
```
```api-test
{
  "method": "POST",
  "path": "/system/website/block/create",
  "headers": {
    "Authorization": "Bearer ${TOKEN}"
  },
  "query": {},
  "body": {
    "site_id": 1,
    "page_code": "home",
    "group_code": "hero",
    "code": "main-hero",
    "name": "首页首屏",
    "type": "hero",
    "title": "德玛仕智慧厨房",
    "subtitle": "整体智慧厨房一站式解决方案服务商",
    "payload": {},
    "media": {},
    "link": {},
    "sort": 100,
    "publish_status": "draft",
    "status": 1
  }
}
```

### 编辑区块

<!-- API_CASE: PUT /system/website/block/update/{id} -->
```jsonc
{
  // method: 请求方法
  "method": "PUT",
  // path: 请求路径，路径参数在案例中替换为可调试值
  "path": "/system/website/block/update/1",
  // headers: 后台接口填写 Authorization，公开接口可为空对象
  "headers": {
    "Authorization": "Bearer ${TOKEN}"
  },
  // query: URL 查询参数
  "query": {},
  // body: 请求体，GET/DELETE 通常为空对象
  "body": {
    "site_id": 1,
    "page_code": "home",
    "group_code": "hero",
    "code": "main-hero",
    "name": "首页首屏",
    "type": "hero",
    "title": "德玛仕智慧厨房",
    "subtitle": "整体智慧厨房一站式解决方案服务商",
    "payload": {},
    "media": {},
    "link": {},
    "sort": 100,
    "publish_status": "draft",
    "status": 1
  }
}
```
```api-test
{
  "method": "PUT",
  "path": "/system/website/block/update/1",
  "headers": {
    "Authorization": "Bearer ${TOKEN}"
  },
  "query": {},
  "body": {
    "site_id": 1,
    "page_code": "home",
    "group_code": "hero",
    "code": "main-hero",
    "name": "首页首屏",
    "type": "hero",
    "title": "德玛仕智慧厨房",
    "subtitle": "整体智慧厨房一站式解决方案服务商",
    "payload": {},
    "media": {},
    "link": {},
    "sort": 100,
    "publish_status": "draft",
    "status": 1
  }
}
```

### 更新区块状态

<!-- API_CASE: PUT /system/website/block/status/{id} -->
```jsonc
{
  // method: 请求方法
  "method": "PUT",
  // path: 请求路径，路径参数在案例中替换为可调试值
  "path": "/system/website/block/status/1",
  // headers: 后台接口填写 Authorization，公开接口可为空对象
  "headers": {
    "Authorization": "Bearer ${TOKEN}"
  },
  // query: URL 查询参数
  "query": {},
  // body: 请求体，GET/DELETE 通常为空对象
  "body": {
    "status": 1
  }
}
```
```api-test
{
  "method": "PUT",
  "path": "/system/website/block/status/1",
  "headers": {
    "Authorization": "Bearer ${TOKEN}"
  },
  "query": {},
  "body": {
    "status": 1
  }
}
```

### 删除区块

<!-- API_CASE: DELETE /system/website/block/delete/{ids} -->
```jsonc
{
  // method: 请求方法
  "method": "DELETE",
  // path: 请求路径，路径参数在案例中替换为可调试值
  "path": "/system/website/block/delete/1,2",
  // headers: 后台接口填写 Authorization，公开接口可为空对象
  "headers": {
    "Authorization": "Bearer ${TOKEN}"
  },
  // query: URL 查询参数
  "query": {},
  // body: 请求体，GET/DELETE 通常为空对象
  "body": {}
}
```
```api-test
{
  "method": "DELETE",
  "path": "/system/website/block/delete/1,2",
  "headers": {
    "Authorization": "Bearer ${TOKEN}"
  },
  "query": {},
  "body": {}
}
```

### 恢复区块

<!-- API_CASE: PUT /system/website/block/recovery/{ids} -->
```jsonc
{
  // method: 请求方法
  "method": "PUT",
  // path: 请求路径，路径参数在案例中替换为可调试值
  "path": "/system/website/block/recovery/1,2",
  // headers: 后台接口填写 Authorization，公开接口可为空对象
  "headers": {
    "Authorization": "Bearer ${TOKEN}"
  },
  // query: URL 查询参数
  "query": {},
  // body: 请求体，GET/DELETE 通常为空对象
  "body": {}
}
```
```api-test
{
  "method": "PUT",
  "path": "/system/website/block/recovery/1,2",
  "headers": {
    "Authorization": "Bearer ${TOKEN}"
  },
  "query": {},
  "body": {}
}
```

### 彻底删除区块

<!-- API_CASE: DELETE /system/website/block/real-delete/{ids} -->
```jsonc
{
  // method: 请求方法
  "method": "DELETE",
  // path: 请求路径，路径参数在案例中替换为可调试值
  "path": "/system/website/block/real-delete/1,2",
  // headers: 后台接口填写 Authorization，公开接口可为空对象
  "headers": {
    "Authorization": "Bearer ${TOKEN}"
  },
  // query: URL 查询参数
  "query": {},
  // body: 请求体，GET/DELETE 通常为空对象
  "body": {}
}
```
```api-test
{
  "method": "DELETE",
  "path": "/system/website/block/real-delete/1,2",
  "headers": {
    "Authorization": "Bearer ${TOKEN}"
  },
  "query": {},
  "body": {}
}
```

### 线索列表

<!-- API_CASE: GET /system/website/lead/index -->
```jsonc
{
  // method: 请求方法
  "method": "GET",
  // path: 请求路径，路径参数在案例中替换为可调试值
  "path": "/system/website/lead/index",
  // headers: 后台接口填写 Authorization，公开接口可为空对象
  "headers": {
    "Authorization": "Bearer ${TOKEN}"
  },
  // query: URL 查询参数
  "query": {
    "page": 1,
    "pageSize": 10,
    "status": "pending"
  },
  // body: 请求体，GET/DELETE 通常为空对象
  "body": {}
}
```
```api-test
{
  "method": "GET",
  "path": "/system/website/lead/index",
  "headers": {
    "Authorization": "Bearer ${TOKEN}"
  },
  "query": {
    "page": 1,
    "pageSize": 10,
    "status": "pending"
  },
  "body": {}
}
```

### 线索详情

<!-- API_CASE: GET /system/website/lead/info/{id} -->
```jsonc
{
  // method: 请求方法
  "method": "GET",
  // path: 请求路径，路径参数在案例中替换为可调试值
  "path": "/system/website/lead/info/1",
  // headers: 后台接口填写 Authorization，公开接口可为空对象
  "headers": {
    "Authorization": "Bearer ${TOKEN}"
  },
  // query: URL 查询参数
  "query": {},
  // body: 请求体，GET/DELETE 通常为空对象
  "body": {}
}
```
```api-test
{
  "method": "GET",
  "path": "/system/website/lead/info/1",
  "headers": {
    "Authorization": "Bearer ${TOKEN}"
  },
  "query": {},
  "body": {}
}
```

### 处理线索

<!-- API_CASE: PUT /system/website/lead/handle/{id} -->
```jsonc
{
  // method: 请求方法
  "method": "PUT",
  // path: 请求路径，路径参数在案例中替换为可调试值
  "path": "/system/website/lead/handle/1",
  // headers: 后台接口填写 Authorization，公开接口可为空对象
  "headers": {
    "Authorization": "Bearer ${TOKEN}"
  },
  // query: URL 查询参数
  "query": {},
  // body: 请求体，GET/DELETE 通常为空对象
  "body": {
    "status": "handled",
    "remark": "已电话沟通"
  }
}
```
```api-test
{
  "method": "PUT",
  "path": "/system/website/lead/handle/1",
  "headers": {
    "Authorization": "Bearer ${TOKEN}"
  },
  "query": {},
  "body": {
    "status": "handled",
    "remark": "已电话沟通"
  }
}
```

### 更新线索状态

<!-- API_CASE: PUT /system/website/lead/status/{id} -->
```jsonc
{
  // method: 请求方法
  "method": "PUT",
  // path: 请求路径，路径参数在案例中替换为可调试值
  "path": "/system/website/lead/status/1",
  // headers: 后台接口填写 Authorization，公开接口可为空对象
  "headers": {
    "Authorization": "Bearer ${TOKEN}"
  },
  // query: URL 查询参数
  "query": {},
  // body: 请求体，GET/DELETE 通常为空对象
  "body": {
    "status": "processing"
  }
}
```
```api-test
{
  "method": "PUT",
  "path": "/system/website/lead/status/1",
  "headers": {
    "Authorization": "Bearer ${TOKEN}"
  },
  "query": {},
  "body": {
    "status": "processing"
  }
}
```

### 删除线索

<!-- API_CASE: DELETE /system/website/lead/delete/{ids} -->
```jsonc
{
  // method: 请求方法
  "method": "DELETE",
  // path: 请求路径，路径参数在案例中替换为可调试值
  "path": "/system/website/lead/delete/1,2",
  // headers: 后台接口填写 Authorization，公开接口可为空对象
  "headers": {
    "Authorization": "Bearer ${TOKEN}"
  },
  // query: URL 查询参数
  "query": {},
  // body: 请求体，GET/DELETE 通常为空对象
  "body": {}
}
```
```api-test
{
  "method": "DELETE",
  "path": "/system/website/lead/delete/1,2",
  "headers": {
    "Authorization": "Bearer ${TOKEN}"
  },
  "query": {},
  "body": {}
}
```

### 开放初始化

<!-- API_CASE: GET /website/bootstrap -->
```jsonc
{
  // method: 请求方法
  "method": "GET",
  // path: 请求路径，路径参数在案例中替换为可调试值
  "path": "/website/bootstrap",
  // headers: 开放接口填写 X-Website-* 签名头
  "headers": {
    "X-Website-Appid": "${WEBSITE_APPID}",
    "X-Website-Timestamp": "${TIMESTAMP}",
    "X-Website-Nonce": "${NONCE}",
    "X-Website-Sign": "${SIGN}"
  },
  // query: URL 查询参数
  "query": {
    "site": "dmsai"
  },
  // body: 请求体，GET/DELETE 通常为空对象
  "body": {}
}
```
```api-test
{
  "method": "GET",
  "path": "/website/bootstrap",
  "headers": {
    "X-Website-Appid": "${WEBSITE_APPID}",
    "X-Website-Timestamp": "${TIMESTAMP}",
    "X-Website-Nonce": "${NONCE}",
    "X-Website-Sign": "${SIGN}"
  },
  "query": {
    "site": "dmsai"
  },
  "body": {}
}
```

### 开放站点

<!-- API_CASE: GET /website/site -->
```jsonc
{
  // method: 请求方法
  "method": "GET",
  // path: 请求路径，路径参数在案例中替换为可调试值
  "path": "/website/site",
  // headers: 开放接口填写 X-Website-* 签名头
  "headers": {
    "X-Website-Appid": "${WEBSITE_APPID}",
    "X-Website-Timestamp": "${TIMESTAMP}",
    "X-Website-Nonce": "${NONCE}",
    "X-Website-Sign": "${SIGN}"
  },
  // query: URL 查询参数
  "query": {
    "site": "dmsai"
  },
  // body: 请求体，GET/DELETE 通常为空对象
  "body": {}
}
```
```api-test
{
  "method": "GET",
  "path": "/website/site",
  "headers": {
    "X-Website-Appid": "${WEBSITE_APPID}",
    "X-Website-Timestamp": "${TIMESTAMP}",
    "X-Website-Nonce": "${NONCE}",
    "X-Website-Sign": "${SIGN}"
  },
  "query": {
    "site": "dmsai"
  },
  "body": {}
}
```

### 开放导航树

<!-- API_CASE: GET /website/nav/tree -->
```jsonc
{
  // method: 请求方法
  "method": "GET",
  // path: 请求路径，路径参数在案例中替换为可调试值
  "path": "/website/nav/tree",
  // headers: 开放接口填写 X-Website-* 签名头
  "headers": {
    "X-Website-Appid": "${WEBSITE_APPID}",
    "X-Website-Timestamp": "${TIMESTAMP}",
    "X-Website-Nonce": "${NONCE}",
    "X-Website-Sign": "${SIGN}"
  },
  // query: URL 查询参数
  "query": {
    "site": "dmsai",
    "position": "top"
  },
  // body: 请求体，GET/DELETE 通常为空对象
  "body": {}
}
```
```api-test
{
  "method": "GET",
  "path": "/website/nav/tree",
  "headers": {
    "X-Website-Appid": "${WEBSITE_APPID}",
    "X-Website-Timestamp": "${TIMESTAMP}",
    "X-Website-Nonce": "${NONCE}",
    "X-Website-Sign": "${SIGN}"
  },
  "query": {
    "site": "dmsai",
    "position": "top"
  },
  "body": {}
}
```

### 开放栏目树

<!-- API_CASE: GET /website/channel/tree -->
```jsonc
{
  // method: 请求方法
  "method": "GET",
  // path: 请求路径，路径参数在案例中替换为可调试值
  "path": "/website/channel/tree",
  // headers: 开放接口填写 X-Website-* 签名头
  "headers": {
    "X-Website-Appid": "${WEBSITE_APPID}",
    "X-Website-Timestamp": "${TIMESTAMP}",
    "X-Website-Nonce": "${NONCE}",
    "X-Website-Sign": "${SIGN}"
  },
  // query: URL 查询参数
  "query": {
    "site": "dmsai"
  },
  // body: 请求体，GET/DELETE 通常为空对象
  "body": {}
}
```
```api-test
{
  "method": "GET",
  "path": "/website/channel/tree",
  "headers": {
    "X-Website-Appid": "${WEBSITE_APPID}",
    "X-Website-Timestamp": "${TIMESTAMP}",
    "X-Website-Nonce": "${NONCE}",
    "X-Website-Sign": "${SIGN}"
  },
  "query": {
    "site": "dmsai"
  },
  "body": {}
}
```

### 开放页面

<!-- API_CASE: GET /website/page -->
```jsonc
{
  // method: 请求方法
  "method": "GET",
  // path: 请求路径，路径参数在案例中替换为可调试值
  "path": "/website/page",
  // headers: 开放接口填写 X-Website-* 签名头
  "headers": {
    "X-Website-Appid": "${WEBSITE_APPID}",
    "X-Website-Timestamp": "${TIMESTAMP}",
    "X-Website-Nonce": "${NONCE}",
    "X-Website-Sign": "${SIGN}"
  },
  // query: URL 查询参数
  "query": {
    "site": "dmsai",
    "route": "/safe/"
  },
  // body: 请求体，GET/DELETE 通常为空对象
  "body": {}
}
```
```api-test
{
  "method": "GET",
  "path": "/website/page",
  "headers": {
    "X-Website-Appid": "${WEBSITE_APPID}",
    "X-Website-Timestamp": "${TIMESTAMP}",
    "X-Website-Nonce": "${NONCE}",
    "X-Website-Sign": "${SIGN}"
  },
  "query": {
    "site": "dmsai",
    "route": "/safe/"
  },
  "body": {}
}
```

### 开放内容列表

<!-- API_CASE: GET /website/content/index -->
```jsonc
{
  // method: 请求方法
  "method": "GET",
  // path: 请求路径，路径参数在案例中替换为可调试值
  "path": "/website/content/index",
  // headers: 开放接口填写 X-Website-* 签名头
  "headers": {
    "X-Website-Appid": "${WEBSITE_APPID}",
    "X-Website-Timestamp": "${TIMESTAMP}",
    "X-Website-Nonce": "${NONCE}",
    "X-Website-Sign": "${SIGN}"
  },
  // query: URL 查询参数
  "query": {
    "site": "dmsai",
    "channel": "news",
    "type": "news",
    "page": 1,
    "pageSize": 10
  },
  // body: 请求体，GET/DELETE 通常为空对象
  "body": {}
}
```
```api-test
{
  "method": "GET",
  "path": "/website/content/index",
  "headers": {
    "X-Website-Appid": "${WEBSITE_APPID}",
    "X-Website-Timestamp": "${TIMESTAMP}",
    "X-Website-Nonce": "${NONCE}",
    "X-Website-Sign": "${SIGN}"
  },
  "query": {
    "site": "dmsai",
    "channel": "news",
    "type": "news",
    "page": 1,
    "pageSize": 10
  },
  "body": {}
}
```

### 开放内容详情

<!-- API_CASE: GET /website/content/info/{id} -->
```jsonc
{
  // method: 请求方法
  "method": "GET",
  // path: 请求路径，路径参数在案例中替换为可调试值
  "path": "/website/content/info/1",
  // headers: 开放接口填写 X-Website-* 签名头
  "headers": {
    "X-Website-Appid": "${WEBSITE_APPID}",
    "X-Website-Timestamp": "${TIMESTAMP}",
    "X-Website-Nonce": "${NONCE}",
    "X-Website-Sign": "${SIGN}"
  },
  // query: URL 查询参数
  "query": {
    "site": "dmsai"
  },
  // body: 请求体，GET/DELETE 通常为空对象
  "body": {}
}
```
```api-test
{
  "method": "GET",
  "path": "/website/content/info/1",
  "headers": {
    "X-Website-Appid": "${WEBSITE_APPID}",
    "X-Website-Timestamp": "${TIMESTAMP}",
    "X-Website-Nonce": "${NONCE}",
    "X-Website-Sign": "${SIGN}"
  },
  "query": {
    "site": "dmsai"
  },
  "body": {}
}
```

### 开放 Slug 内容

<!-- API_CASE: GET /website/content/slug/{slug} -->
```jsonc
{
  // method: 请求方法
  "method": "GET",
  // path: 请求路径，路径参数在案例中替换为可调试值
  "path": "/website/content/slug/demo-news",
  // headers: 开放接口填写 X-Website-* 签名头
  "headers": {
    "X-Website-Appid": "${WEBSITE_APPID}",
    "X-Website-Timestamp": "${TIMESTAMP}",
    "X-Website-Nonce": "${NONCE}",
    "X-Website-Sign": "${SIGN}"
  },
  // query: URL 查询参数
  "query": {
    "site": "dmsai"
  },
  // body: 请求体，GET/DELETE 通常为空对象
  "body": {}
}
```
```api-test
{
  "method": "GET",
  "path": "/website/content/slug/demo-news",
  "headers": {
    "X-Website-Appid": "${WEBSITE_APPID}",
    "X-Website-Timestamp": "${TIMESTAMP}",
    "X-Website-Nonce": "${NONCE}",
    "X-Website-Sign": "${SIGN}"
  },
  "query": {
    "site": "dmsai"
  },
  "body": {}
}
```

### 开放区块列表

<!-- API_CASE: GET /website/block/list -->
```jsonc
{
  // method: 请求方法
  "method": "GET",
  // path: 请求路径，路径参数在案例中替换为可调试值
  "path": "/website/block/list",
  // headers: 开放接口填写 X-Website-* 签名头
  "headers": {
    "X-Website-Appid": "${WEBSITE_APPID}",
    "X-Website-Timestamp": "${TIMESTAMP}",
    "X-Website-Nonce": "${NONCE}",
    "X-Website-Sign": "${SIGN}"
  },
  // query: URL 查询参数
  "query": {
    "site": "dmsai",
    "page_code": "home"
  },
  // body: 请求体，GET/DELETE 通常为空对象
  "body": {}
}
```
```api-test
{
  "method": "GET",
  "path": "/website/block/list",
  "headers": {
    "X-Website-Appid": "${WEBSITE_APPID}",
    "X-Website-Timestamp": "${TIMESTAMP}",
    "X-Website-Nonce": "${NONCE}",
    "X-Website-Sign": "${SIGN}"
  },
  "query": {
    "site": "dmsai",
    "page_code": "home"
  },
  "body": {}
}
```

### 开放线索提交

<!-- API_CASE: POST /website/lead/create -->
```jsonc
{
  // method: 请求方法
  "method": "POST",
  // path: 请求路径，路径参数在案例中替换为可调试值
  "path": "/website/lead/create",
  // headers: 开放接口填写 X-Website-* 签名头
  "headers": {
    "X-Website-Appid": "${WEBSITE_APPID}",
    "X-Website-Timestamp": "${TIMESTAMP}",
    "X-Website-Nonce": "${NONCE}",
    "X-Website-Sign": "${SIGN}"
  },
  // query: URL 查询参数
  "query": {
    "site": "dmsai"
  },
  // body: 请求体，GET/DELETE 通常为空对象
  "body": {
    "name": "张三",
    "mobile": "13800138000",
    "email": "demo@example.com",
    "company": "演示单位",
    "subject": "智慧厨房咨询",
    "content": "想了解智慧厨房方案",
    "source_url": "https://dmsai.cn/"
  }
}
```
```api-test
{
  "method": "POST",
  "path": "/website/lead/create",
  "headers": {
    "X-Website-Appid": "${WEBSITE_APPID}",
    "X-Website-Timestamp": "${TIMESTAMP}",
    "X-Website-Nonce": "${NONCE}",
    "X-Website-Sign": "${SIGN}"
  },
  "query": {
    "site": "dmsai"
  },
  "body": {
    "name": "张三",
    "mobile": "13800138000",
    "email": "demo@example.com",
    "company": "演示单位",
    "subject": "智慧厨房咨询",
    "content": "想了解智慧厨房方案",
    "source_url": "https://dmsai.cn/"
  }
}
```

<!-- API_CASES_END -->
