<!-- DOC_META: plugin=points source=plugin/Points distribution=member_only -->
# 积分管理接口

> **会员授权 / 不随开源版源码分发：** 本页为会员授权插件文档，公开文档站展示接口与使用说明，但开源仓不包含该插件源码或 ZIP，安装使用需会员授权或内部交付。

Points 插件是会员专属应用，不随 SmartAdmin 开源版开放分发；仅会员授权环境可安装和使用。

Points 插件提供基于钉钉组织、钉钉加班审批和企业内部礼品库的积分兑换闭环。后台接口使用 System Token 和 `points.*` 权限码；员工端接口使用 `Plugin\Points\Model\PointsMember` Token，员工身份只能由钉钉免登换取，不接受前端直接提交钉钉 UserId 冒用。

## 后台钉钉配置与审批

| 方法 | 路径 | 权限码 | 说明 |
|---|---|---|---|
| GET | `/system/points/dingtalk/config` | `points.dingtalk.index` | 获取当前租户钉钉积分配置，Client ID、Client Secret 和回调 Token 只回显掩码。 |
| PUT | `/system/points/dingtalk/config` | `points.dingtalk.config` | 保存 CorpId、Client ID、Client Secret、AgentId、员工端地址、审批流程和字段映射。 |
| POST | `/system/points/dingtalk/sync` | `points.dingtalk.sync` | 同步钉钉部门员工到 Points 自有员工快照，不写入 System 用户。 |
| GET | `/system/points/dingtalk/sync-logs` | `points.dingtalk.log` | 查询组织同步日志。 |
| GET | `/system/points/dingtalk/user-binds` | `points.dingtalk.sync` | 查询钉钉员工绑定快照。 |
| GET | `/system/points/dingtalk/notify-logs` | `points.dingtalk.log` | 查询钉钉通知日志。 |
| GET | `/system/points/dingtalk/approvals` | `points.dingtalk.approval-import` | 查询加班审批导入记录。 |
| POST | `/system/points/dingtalk/approval-import` | `points.dingtalk.approval-import` | 按审批实例 ID 或时间范围补偿导入已通过加班审批。 |

## 后台积分、礼品与兑换

| 方法 | 路径 | 权限码 | 说明 |
|---|---|---|---|
| GET | `/system/points/rules/index` | `points.rule.index` | 查询加班审批积分规则。 |
| GET | `/system/points/rules/info/{id}` | `points.rule.index` | 获取积分规则详情。 |
| POST | `/system/points/rules/create` | `points.rule.create` | 新增加班审批积分规则。 |
| PUT | `/system/points/rules/update/{id}` | `points.rule.update` | 编辑积分规则。 |
| PUT | `/system/points/rules/status/{id}` | `points.rule.status` | 启用或禁用积分规则。 |
| DELETE | `/system/points/rules/delete/{ids}` | `points.rule.delete` | 删除积分规则。 |
| GET | `/system/points/members/index` | `points.member.index` | 查询员工积分账户和余额统计。 |
| GET | `/system/points/members/info/{id}` | `points.member.index` | 获取员工积分账户详情。 |
| PUT | `/system/points/members/status/{id}` | `points.member.status` | 启用或禁用员工积分账户。 |
| POST | `/system/points/members/adjust/{id}` | `points.member.adjust` | 手工调整员工积分，必须追加积分流水。 |
| GET | `/system/points/ledger/index` | `points.ledger.index` | 查询不可变积分流水。 |
| GET | `/system/points/gifts/index` | `points.gift.index` | 查询礼品列表。 |
| GET | `/system/points/gifts/info/{id}` | `points.gift.index` | 获取礼品详情。 |
| POST | `/system/points/gifts/create` | `points.gift.create` | 新增企业内部礼品。 |
| PUT | `/system/points/gifts/update/{id}` | `points.gift.update` | 编辑礼品积分价格、库存和说明。 |
| PUT | `/system/points/gifts/status/{id}` | `points.gift.status` | 上架或下架礼品。 |
| POST | `/system/points/gifts/stock/{id}` | `points.gift.stock` | 调整礼品库存。 |
| DELETE | `/system/points/gifts/delete/{ids}` | `points.gift.delete` | 删除礼品。 |
| GET | `/system/points/exchanges/index` | `points.exchange.index` | 查询兑换单。 |
| GET | `/system/points/exchanges/info/{id}` | `points.exchange.index` | 获取兑换详情。 |
| POST | `/system/points/exchanges/approve/{id}` | `points.exchange.audit` | 审核通过兑换。 |
| POST | `/system/points/exchanges/reject/{id}` | `points.exchange.audit` | 驳回兑换并退回积分和库存。 |
| POST | `/system/points/exchanges/deliver/{id}` | `points.exchange.deliver` | 确认礼品已发放，发放后不可撤销。 |
| GET | `/system/points/reports/overview` | `points.report.index` | 查询积分发放、消耗、余额和兑换概览。 |

## 员工端接口

| 方法 | 路径 | 认证 | 说明 |
|---|---|---|---|
| GET | `/points/account/auth/dingtalk-config` | 公开 | 获取钉钉免登所需 CorpId，不返回服务端密钥。 |
| POST | `/points/account/auth/dingtalk-login` | 公开 | 使用钉钉 auth code 登录积分员工端。 |
| POST | `/points/account/auth/refresh` | Points Token | 刷新员工端 Token。 |
| POST | `/points/account/auth/profile` | Points Token | 获取当前积分员工资料。 |
| GET | `/points/account/auth/codes` | Points Token | 获取员工端权限码。 |
| POST | `/points/account/auth/logout` | Points Token | 登出员工端。 |
| GET | `/points/me/balance` | Points Token | 获取我的积分余额、累计获得和累计消耗。 |
| GET | `/points/me/ledger` | Points Token | 查询我的积分流水。 |
| GET | `/points/gifts/index` | Points Token | 查询已上架礼品。 |
| GET | `/points/gifts/info/{id}` | Points Token | 获取已上架礼品详情。 |
| GET | `/points/exchanges/index` | Points Token | 查询我的兑换记录。 |
| POST | `/points/exchanges/create` | Points Token | 提交礼品兑换，立即扣减积分并占用库存。 |
| POST | `/points/exchanges/cancel/{id}` | Points Token | 取消待审核兑换并退回积分和库存。 |
| POST | `/points/dingtalk/approval-callback` | 回调 Token | 钉钉审批事件回调，校验配置 Token 后导入审批实例。 |

## 接口案例

<!-- API_CASES_START -->

以下示例用于 Docsify 在线调试器，路径中的 `{id}`、`{ids}` 等占位符需要替换为真实业务值。

### 1. GET /system/points/dingtalk/config

<!-- API_CASE: GET /system/points/dingtalk/config -->

`GET /system/points/dingtalk/config`

```jsonc
{
  // method: HTTP 方法，必须与接口表格中的方法一致。
  "method": "GET",
  // path: 请求路径，占位符按实际 ID 或业务参数替换。
  "path": "/system/points/dingtalk/config",
  // headers: 请求头，登录或权限接口需要 Authorization。
  "headers": {
    "Authorization": "Bearer <token>",
    "Content-Type": "application/json"
  },
  // query: Query 参数，列表接口可传分页和筛选条件。
  "query": {},
  // body: Body 请求体，写接口按业务表单提交字段。
  "body": ""
}
```

在线调试：

```api-test
{
  "method": "GET",
  "path": "/system/points/dingtalk/config",
  "headers": {
    "Authorization": "Bearer <token>",
    "Content-Type": "application/json"
  },
  "query": {},
  "body": ""
}
```
响应示例：

```jsonc
{
  // code: 业务响应码；成功固定为 200，认证、权限、路由和业务异常分别使用 401/403/404/500。
  "code": 200,
  // info: 业务提示文案；字段名固定为 info，不使用 message 作为主字段。
  "info": "更新成功",
  // data: GET /system/points/dingtalk/config响应数据；下方字段为本接口可对接的稳定结构。
  "data": {
    // id: 主键ID。
    "id": 1,
    // tenant_id: 租户ID。
    "tenant_id": 1,
    // name: 配置名称。
    "name": "示例名称",
    // status: 状态(1启用,0禁用)。
    "status": 1,
    // created_by: 创建者。
    "created_by": 1,
    // updated_by: 更新者。
    "updated_by": 1,
    // corp_id: 钉钉 CorpId（组织 ID）。
    "corp_id": 1,
    // agent_id: 钉钉原企业内部应用 AgentId。
    "agent_id": 1,
    // points_base_url: 积分员工端访问地址。
    "points_base_url": "https://example.com/demo.png",
    // root_dept_id: 同步根部门ID。
    "root_dept_id": 1,
    // approval_process_code: 加班审批流程编码。
    "approval_process_code": "示例值",
    // approval_hours_field: 审批单加班时长字段。
    "approval_hours_field": "示例值",
    // approval_user_field: 审批单员工字段。
    "approval_user_field": "示例值",
    // callback_token: 钉钉回调校验 Token 密文。
    "callback_token": "示例值",
    // sync_enabled: 是否允许同步。
    "sync_enabled": "示例值",
    // notify_enabled: 是否允许发送通知。
    "notify_enabled": "示例值",
    // created_at: 创建时间。
    "created_at": "2026-05-29 10:00:00",
    // updated_at: 更新时间。
    "updated_at": "2026-05-29 10:00:00"
  },
  // path: 当前请求路径；调试时应与实际 Path 保持一致。
  "path": "/system/points/dingtalk/config"
}
```


### 2. PUT /system/points/dingtalk/config

<!-- API_CASE: PUT /system/points/dingtalk/config -->

`PUT /system/points/dingtalk/config`

```jsonc
{
  // method: HTTP 方法，必须与接口表格中的方法一致。
  "method": "PUT",
  // path: 请求路径，占位符按实际 ID 或业务参数替换。
  "path": "/system/points/dingtalk/config",
  // headers: 请求头，登录或权限接口需要 Authorization。
  "headers": {
    "Authorization": "Bearer <token>",
    "Content-Type": "application/json"
  },
  // query: Query 参数，列表接口可传分页和筛选条件。
  "query": {},
  // body: Body 请求体，写接口按业务表单提交字段。
  "body": {
    "name": "默认积分钉钉配置",
    "corp_id": "ding-corp-id",
    "app_key": "ding-client-id",
    "app_secret": "ding-client-secret",
    "agent_id": "123456",
    "points_base_url": "https://points.example.com",
    "root_dept_id": "1",
    "approval_process_code": "PROC-OVERTIME",
    "approval_hours_field": "加班时长",
    "callback_token": "callback-token",
    "sync_enabled": 1,
    "notify_enabled": 1,
    "disabled_missing_users": 0,
    "status": 1
  }
}
```

在线调试：

```api-test
{
  "method": "PUT",
  "path": "/system/points/dingtalk/config",
  "headers": {
    "Authorization": "Bearer <token>",
    "Content-Type": "application/json"
  },
  "query": {},
  "body": {
    "name": "默认积分钉钉配置",
    "corp_id": "ding-corp-id",
    "app_key": "ding-client-id",
    "app_secret": "ding-client-secret",
    "agent_id": "123456",
    "points_base_url": "https://points.example.com",
    "root_dept_id": "1",
    "approval_process_code": "PROC-OVERTIME",
    "approval_hours_field": "加班时长",
    "callback_token": "callback-token",
    "sync_enabled": 1,
    "notify_enabled": 1,
    "disabled_missing_users": 0,
    "status": 1
  }
}
```
响应示例：

```jsonc
{
  // code: 业务响应码；成功固定为 200，认证、权限、路由和业务异常分别使用 401/403/404/500。
  "code": 200,
  // info: 业务提示文案；字段名固定为 info，不使用 message 作为主字段。
  "info": "更新成功",
  // data: PUT /system/points/dingtalk/config响应数据；下方字段为本接口可对接的稳定结构。
  "data": {
    // id: 主键ID。
    "id": 1,
    // tenant_id: 租户ID。
    "tenant_id": 1,
    // name: 配置名称。
    "name": "示例名称",
    // status: 状态(1启用,0禁用)。
    "status": 1,
    // created_by: 创建者。
    "created_by": 1,
    // updated_by: 更新者。
    "updated_by": 1,
    // corp_id: 钉钉 CorpId（组织 ID）。
    "corp_id": 1,
    // agent_id: 钉钉原企业内部应用 AgentId。
    "agent_id": 1,
    // points_base_url: 积分员工端访问地址。
    "points_base_url": "https://example.com/demo.png",
    // root_dept_id: 同步根部门ID。
    "root_dept_id": 1,
    // approval_process_code: 加班审批流程编码。
    "approval_process_code": "示例值",
    // approval_hours_field: 审批单加班时长字段。
    "approval_hours_field": "示例值",
    // approval_user_field: 审批单员工字段。
    "approval_user_field": "示例值",
    // callback_token: 钉钉回调校验 Token 密文。
    "callback_token": "示例值",
    // sync_enabled: 是否允许同步。
    "sync_enabled": "示例值",
    // notify_enabled: 是否允许发送通知。
    "notify_enabled": "示例值",
    // created_at: 创建时间。
    "created_at": "2026-05-29 10:00:00",
    // updated_at: 更新时间。
    "updated_at": "2026-05-29 10:00:00"
  },
  // path: 当前请求路径；调试时应与实际 Path 保持一致。
  "path": "/system/points/dingtalk/config"
}
```


### 3. POST /system/points/dingtalk/sync

<!-- API_CASE: POST /system/points/dingtalk/sync -->

`POST /system/points/dingtalk/sync`

```jsonc
{
  // method: HTTP 方法，必须与接口表格中的方法一致。
  "method": "POST",
  // path: 请求路径，占位符按实际 ID 或业务参数替换。
  "path": "/system/points/dingtalk/sync",
  // headers: 请求头，登录或权限接口需要 Authorization。
  "headers": {
    "Authorization": "Bearer <token>",
    "Content-Type": "application/json"
  },
  // query: Query 参数，列表接口可传分页和筛选条件。
  "query": {},
  // body: Body 请求体，写接口按业务表单提交字段。
  "body": {
    "config_id": 1
  }
}
```

在线调试：

```api-test
{
  "method": "POST",
  "path": "/system/points/dingtalk/sync",
  "headers": {
    "Authorization": "Bearer <token>",
    "Content-Type": "application/json"
  },
  "query": {},
  "body": {
    "config_id": 1
  }
}
```
响应示例：

```jsonc
{
  // code: 业务响应码；成功固定为 200，认证、权限、路由和业务异常分别使用 401/403/404/500。
  "code": 200,
  // info: 业务提示文案；字段名固定为 info，不使用 message 作为主字段。
  "info": "获取成功",
  // data: POST /system/points/dingtalk/sync响应数据；下方字段为本接口可对接的稳定结构。
  "data": true,
  // path: 当前请求路径；调试时应与实际 Path 保持一致。
  "path": "/system/points/dingtalk/sync"
}
```


### 4. GET /system/points/dingtalk/sync-logs

<!-- API_CASE: GET /system/points/dingtalk/sync-logs -->

`GET /system/points/dingtalk/sync-logs`

```jsonc
{
  // method: HTTP 方法，必须与接口表格中的方法一致。
  "method": "GET",
  // path: 请求路径，占位符按实际 ID 或业务参数替换。
  "path": "/system/points/dingtalk/sync-logs",
  // headers: 请求头，登录或权限接口需要 Authorization。
  "headers": {
    "Authorization": "Bearer <token>",
    "Content-Type": "application/json"
  },
  // query: Query 参数，列表接口可传分页和筛选条件。
  "query": {},
  // body: Body 请求体，写接口按业务表单提交字段。
  "body": ""
}
```

在线调试：

```api-test
{
  "method": "GET",
  "path": "/system/points/dingtalk/sync-logs",
  "headers": {
    "Authorization": "Bearer <token>",
    "Content-Type": "application/json"
  },
  "query": {},
  "body": ""
}
```
响应示例：

```jsonc
{
  // code: 业务响应码；成功固定为 200，认证、权限、路由和业务异常分别使用 401/403/404/500。
  "code": 200,
  // info: 业务提示文案；字段名固定为 info，不使用 message 作为主字段。
  "info": "获取成功",
  // data: GET /system/points/dingtalk/sync-logs响应数据；下方字段为本接口可对接的稳定结构。
  "data": {
    // items: 当前页数据列表；数组元素为本接口对应业务对象。
    "items": [
      {
        // id: 主键ID。
        "id": 1,
        // tenant_id: 租户ID。
        "tenant_id": 1,
        // status: 状态(1启用,0禁用)。
        "status": 1,
        // created_by: 创建者。
        "created_by": 1,
        // updated_by: 更新者。
        "updated_by": 1,
        // config_id: 钉钉配置ID。
        "config_id": 1,
        // sync_type: 同步类型。
        "sync_type": "示例值",
        // sync_status: 同步状态。
        "sync_status": "示例值",
        // success_count: 成功数量。
        "success_count": 1,
        // failure_count: 失败数量。
        "failure_count": 1,
        // created_at: 创建时间。
        "created_at": "2026-05-29 10:00:00",
        // updated_at: 更新时间。
        "updated_at": "2026-05-29 10:00:00"
      }
    ],
    // pageInfo: 分页信息；标准分页接口由 CoreMapper 返回 total/totalPage/currentPage。
    "pageInfo": {
      // total: 符合当前权限、租户和筛选条件的总记录数。
      "total": 0,
      // totalPage: 按当前 pageSize 计算的总页数。
      "totalPage": 0,
      // currentPage: 当前页码，从 1 开始。
      "currentPage": 1
    }
  },
  // path: 当前请求路径；调试时应与实际 Path 保持一致。
  "path": "/system/points/dingtalk/sync-logs"
}
```


### 5. GET /system/points/dingtalk/user-binds

<!-- API_CASE: GET /system/points/dingtalk/user-binds -->

`GET /system/points/dingtalk/user-binds`

```jsonc
{
  // method: HTTP 方法，必须与接口表格中的方法一致。
  "method": "GET",
  // path: 请求路径，占位符按实际 ID 或业务参数替换。
  "path": "/system/points/dingtalk/user-binds",
  // headers: 请求头，登录或权限接口需要 Authorization。
  "headers": {
    "Authorization": "Bearer <token>",
    "Content-Type": "application/json"
  },
  // query: Query 参数，列表接口可传分页和筛选条件。
  "query": {
    "page": 1,
    "pageSize": 15
  },
  // body: Body 请求体，写接口按业务表单提交字段。
  "body": ""
}
```

在线调试：

```api-test
{
  "method": "GET",
  "path": "/system/points/dingtalk/user-binds",
  "headers": {
    "Authorization": "Bearer <token>",
    "Content-Type": "application/json"
  },
  "query": {
    "page": 1,
    "pageSize": 15
  },
  "body": ""
}
```
响应示例：

```jsonc
{
  // code: 业务响应码；成功固定为 200，认证、权限、路由和业务异常分别使用 401/403/404/500。
  "code": 200,
  // info: 业务提示文案；字段名固定为 info，不使用 message 作为主字段。
  "info": "获取成功",
  // data: GET /system/points/dingtalk/user-binds响应数据；下方字段为本接口可对接的稳定结构。
  "data": {
    // items: 当前页数据列表；数组元素为本接口对应业务对象。
    "items": [
      {
        // id: 主键ID。
        "id": 1,
        // tenant_id: 租户ID。
        "tenant_id": 1,
        // name: 用户姓名。
        "name": "示例名称",
        // status: 状态(1启用,0禁用)。
        "status": 1,
        // created_by: 创建者。
        "created_by": 1,
        // updated_by: 更新者。
        "updated_by": 1,
        // dingtalk_userid: 钉钉用户ID。
        "dingtalk_userid": "示例值",
        // unionid: 钉钉 UnionID。
        "unionid": "demo_code",
        // member_id: 积分员工ID。
        "member_id": 1,
        // job_number: 工号。
        "job_number": "示例值",
        // created_at: 创建时间。
        "created_at": "2026-05-29 10:00:00",
        // updated_at: 更新时间。
        "updated_at": "2026-05-29 10:00:00"
      }
    ],
    // pageInfo: 分页信息；标准分页接口由 CoreMapper 返回 total/totalPage/currentPage。
    "pageInfo": {
      // total: 符合当前权限、租户和筛选条件的总记录数。
      "total": 0,
      // totalPage: 按当前 pageSize 计算的总页数。
      "totalPage": 0,
      // currentPage: 当前页码，从 1 开始。
      "currentPage": 1
    }
  },
  // path: 当前请求路径；调试时应与实际 Path 保持一致。
  "path": "/system/points/dingtalk/user-binds"
}
```


### 6. GET /system/points/dingtalk/notify-logs

<!-- API_CASE: GET /system/points/dingtalk/notify-logs -->

`GET /system/points/dingtalk/notify-logs`

```jsonc
{
  // method: HTTP 方法，必须与接口表格中的方法一致。
  "method": "GET",
  // path: 请求路径，占位符按实际 ID 或业务参数替换。
  "path": "/system/points/dingtalk/notify-logs",
  // headers: 请求头，登录或权限接口需要 Authorization。
  "headers": {
    "Authorization": "Bearer <token>",
    "Content-Type": "application/json"
  },
  // query: Query 参数，列表接口可传分页和筛选条件。
  "query": {},
  // body: Body 请求体，写接口按业务表单提交字段。
  "body": ""
}
```

在线调试：

```api-test
{
  "method": "GET",
  "path": "/system/points/dingtalk/notify-logs",
  "headers": {
    "Authorization": "Bearer <token>",
    "Content-Type": "application/json"
  },
  "query": {},
  "body": ""
}
```
响应示例：

```jsonc
{
  // code: 业务响应码；成功固定为 200，认证、权限、路由和业务异常分别使用 401/403/404/500。
  "code": 200,
  // info: 业务提示文案；字段名固定为 info，不使用 message 作为主字段。
  "info": "获取成功",
  // data: GET /system/points/dingtalk/notify-logs响应数据；下方字段为本接口可对接的稳定结构。
  "data": {
    // id: 主键ID。
    "id": 1,
    // tenant_id: 租户ID。
    "tenant_id": 1,
    // task_id: 钉钉异步任务ID。
    "task_id": 1,
    // status: 状态(1启用,0禁用)。
    "status": 1,
    // created_at: 创建时间。
    "created_at": "2026-05-29 10:00:00",
    // updated_at: 更新时间。
    "updated_at": "2026-05-29 10:00:00",
    // created_by: 创建者。
    "created_by": 1,
    // updated_by: 更新者。
    "updated_by": 1,
    // config_id: 钉钉配置ID。
    "config_id": 1,
    // member_id: 员工ID。
    "member_id": 1,
    // dingtalk_userid: 钉钉用户ID。
    "dingtalk_userid": "示例值",
    // message_type: 消息类型。
    "message_type": "示例值",
    // notify_status: 发送状态。
    "notify_status": "示例值",
    // request_payload: 请求 JSON。
    "request_payload": "示例值",
    // response_payload: 响应 JSON。
    "response_payload": "示例值",
    // error_detail: 错误详情。
    "error_detail": "示例值",
    // sent_at: 发送时间。
    "sent_at": "2026-05-29 10:00:00",
    // result_checked_at: 结果查询时间。
    "result_checked_at": "2026-05-29 10:00:00"
  },
  // path: 当前请求路径；调试时应与实际 Path 保持一致。
  "path": "/system/points/dingtalk/notify-logs"
}
```


### 7. GET /system/points/dingtalk/approvals

<!-- API_CASE: GET /system/points/dingtalk/approvals -->

`GET /system/points/dingtalk/approvals`

```jsonc
{
  // method: HTTP 方法，必须与接口表格中的方法一致。
  "method": "GET",
  // path: 请求路径，占位符按实际 ID 或业务参数替换。
  "path": "/system/points/dingtalk/approvals",
  // headers: 请求头，登录或权限接口需要 Authorization。
  "headers": {
    "Authorization": "Bearer <token>",
    "Content-Type": "application/json"
  },
  // query: Query 参数，列表接口可传分页和筛选条件。
  "query": {
    "page": 1,
    "pageSize": 15
  },
  // body: Body 请求体，写接口按业务表单提交字段。
  "body": ""
}
```

在线调试：

```api-test
{
  "method": "GET",
  "path": "/system/points/dingtalk/approvals",
  "headers": {
    "Authorization": "Bearer <token>",
    "Content-Type": "application/json"
  },
  "query": {
    "page": 1,
    "pageSize": 15
  },
  "body": ""
}
```
响应示例：

```jsonc
{
  // code: 业务响应码；成功固定为 200，认证、权限、路由和业务异常分别使用 401/403/404/500。
  "code": 200,
  // info: 业务提示文案；字段名固定为 info，不使用 message 作为主字段。
  "info": "获取成功",
  // data: GET /system/points/dingtalk/approvals响应数据；下方字段为本接口可对接的稳定结构。
  "data": {
    // id: 配置或业务对象 ID。
    "id": 1,
    // name: 配置或业务对象名称。
    "name": "示例名称",
    // status: 状态值。
    "status": 1
  },
  // path: 当前请求路径；调试时应与实际 Path 保持一致。
  "path": "/system/points/dingtalk/approvals"
}
```


### 8. POST /system/points/dingtalk/approval-import

<!-- API_CASE: POST /system/points/dingtalk/approval-import -->

`POST /system/points/dingtalk/approval-import`

```jsonc
{
  // method: HTTP 方法，必须与接口表格中的方法一致。
  "method": "POST",
  // path: 请求路径，占位符按实际 ID 或业务参数替换。
  "path": "/system/points/dingtalk/approval-import",
  // headers: 请求头，登录或权限接口需要 Authorization。
  "headers": {
    "Authorization": "Bearer <token>",
    "Content-Type": "application/json"
  },
  // query: Query 参数，列表接口可传分页和筛选条件。
  "query": {},
  // body: Body 请求体，写接口按业务表单提交字段。
  "body": {
    "config_id": 1,
    "approval_instance_id": "PROC-INST-001",
    "start_at": "2026-05-01 00:00:00",
    "end_at": "2026-05-27 23:59:59"
  }
}
```

在线调试：

```api-test
{
  "method": "POST",
  "path": "/system/points/dingtalk/approval-import",
  "headers": {
    "Authorization": "Bearer <token>",
    "Content-Type": "application/json"
  },
  "query": {},
  "body": {
    "config_id": 1,
    "approval_instance_id": "PROC-INST-001",
    "start_at": "2026-05-01 00:00:00",
    "end_at": "2026-05-27 23:59:59"
  }
}
```
响应示例：

```jsonc
{
  // code: 业务响应码；成功固定为 200，认证、权限、路由和业务异常分别使用 401/403/404/500。
  "code": 200,
  // info: 业务提示文案；字段名固定为 info，不使用 message 作为主字段。
  "info": "获取成功",
  // data: POST /system/points/dingtalk/approval-import响应数据；下方字段为本接口可对接的稳定结构。
  "data": {
    // id: 新建记录 ID。
    "id": 1,
    // config_id: config id。
    "config_id": 1,
    // approval_instance_id: approval instance id。
    "approval_instance_id": "PROC-INST-001",
    // start_at: start at。
    "start_at": "2026-05-01 00:00:00",
    // end_at: end at。
    "end_at": "2026-05-27 23:59:59"
  },
  // path: 当前请求路径；调试时应与实际 Path 保持一致。
  "path": "/system/points/dingtalk/approval-import"
}
```


### 9. GET /system/points/rules/index

<!-- API_CASE: GET /system/points/rules/index -->

`GET /system/points/rules/index`

```jsonc
{
  // method: HTTP 方法，必须与接口表格中的方法一致。
  "method": "GET",
  // path: 请求路径，占位符按实际 ID 或业务参数替换。
  "path": "/system/points/rules/index",
  // headers: 请求头，登录或权限接口需要 Authorization。
  "headers": {
    "Authorization": "Bearer <token>",
    "Content-Type": "application/json"
  },
  // query: Query 参数，列表接口可传分页和筛选条件。
  "query": {
    "page": 1,
    "pageSize": 15
  },
  // body: Body 请求体，写接口按业务表单提交字段。
  "body": ""
}
```

在线调试：

```api-test
{
  "method": "GET",
  "path": "/system/points/rules/index",
  "headers": {
    "Authorization": "Bearer <token>",
    "Content-Type": "application/json"
  },
  "query": {
    "page": 1,
    "pageSize": 15
  },
  "body": ""
}
```
响应示例：

```jsonc
{
  // code: 业务响应码；成功固定为 200，认证、权限、路由和业务异常分别使用 401/403/404/500。
  "code": 200,
  // info: 业务提示文案；字段名固定为 info，不使用 message 作为主字段。
  "info": "获取成功",
  // data: GET /system/points/rules/index响应数据；下方字段为本接口可对接的稳定结构。
  "data": {
    // items: 当前页数据列表；数组元素为本接口对应业务对象。
    "items": [
      {
        // id: 主键ID。
        "id": 1,
        // tenant_id: 租户ID。
        "tenant_id": 1,
        // name: 规则名称。
        "name": "示例名称",
        // status: 状态(1启用,0禁用)。
        "status": 1,
        // remark: 备注。
        "remark": "示例内容",
        // created_by: 创建者。
        "created_by": 1,
        // updated_by: 更新者。
        "updated_by": 1,
        // source_type: 积分来源类型。
        "source_type": "示例值",
        // process_code: 审批流程编码。
        "process_code": "示例值",
        // hours_field: 审批时长字段。
        "hours_field": "示例值",
        // created_at: 创建时间。
        "created_at": "2026-05-29 10:00:00",
        // updated_at: 更新时间。
        "updated_at": "2026-05-29 10:00:00"
      }
    ],
    // pageInfo: 分页信息；标准分页接口由 CoreMapper 返回 total/totalPage/currentPage。
    "pageInfo": {
      // total: 符合当前权限、租户和筛选条件的总记录数。
      "total": 0,
      // totalPage: 按当前 pageSize 计算的总页数。
      "totalPage": 0,
      // currentPage: 当前页码，从 1 开始。
      "currentPage": 1
    }
  },
  // path: 当前请求路径；调试时应与实际 Path 保持一致。
  "path": "/system/points/rules/index"
}
```


### 10. GET /system/points/rules/info/{id}

<!-- API_CASE: GET /system/points/rules/info/{id} -->

`GET /system/points/rules/info/{id}`

```jsonc
{
  // method: HTTP 方法，必须与接口表格中的方法一致。
  "method": "GET",
  // path: 请求路径，占位符按实际 ID 或业务参数替换。
  "path": "/system/points/rules/info/{id}",
  // headers: 请求头，登录或权限接口需要 Authorization。
  "headers": {
    "Authorization": "Bearer <token>",
    "Content-Type": "application/json"
  },
  // query: Query 参数，列表接口可传分页和筛选条件。
  "query": {},
  // body: Body 请求体，写接口按业务表单提交字段。
  "body": ""
}
```

在线调试：

```api-test
{
  "method": "GET",
  "path": "/system/points/rules/info/{id}",
  "headers": {
    "Authorization": "Bearer <token>",
    "Content-Type": "application/json"
  },
  "query": {},
  "body": ""
}
```
响应示例：

```jsonc
{
  // code: 业务响应码；成功固定为 200，认证、权限、路由和业务异常分别使用 401/403/404/500。
  "code": 200,
  // info: 业务提示文案；字段名固定为 info，不使用 message 作为主字段。
  "info": "获取成功",
  // data: GET /system/points/rules/info/{id}响应数据；下方字段为本接口可对接的稳定结构。
  "data": {
    // id: 主键ID。
    "id": 1,
    // tenant_id: 租户ID。
    "tenant_id": 1,
    // name: 规则名称。
    "name": "示例名称",
    // status: 状态(1启用,0禁用)。
    "status": 1,
    // remark: 备注。
    "remark": "示例内容",
    // created_at: 创建时间。
    "created_at": "2026-05-29 10:00:00",
    // updated_at: 更新时间。
    "updated_at": "2026-05-29 10:00:00",
    // created_by: 创建者。
    "created_by": 1,
    // updated_by: 更新者。
    "updated_by": 1,
    // source_type: 积分来源类型。
    "source_type": "示例值",
    // process_code: 审批流程编码。
    "process_code": "示例值",
    // hours_field: 审批时长字段。
    "hours_field": "示例值",
    // points_per_hour: 每小时积分。
    "points_per_hour": "示例值",
    // min_hours: 最小有效小时。
    "min_hours": "示例值",
    // max_points_per_order: 单单最高积分。
    "max_points_per_order": "示例值"
  },
  // path: 当前请求路径；调试时应与实际 Path 保持一致。
  "path": "/system/points/rules/info/{id}"
}
```


### 11. POST /system/points/rules/create

<!-- API_CASE: POST /system/points/rules/create -->

`POST /system/points/rules/create`

```jsonc
{
  // method: HTTP 方法，必须与接口表格中的方法一致。
  "method": "POST",
  // path: 请求路径，占位符按实际 ID 或业务参数替换。
  "path": "/system/points/rules/create",
  // headers: 请求头，登录或权限接口需要 Authorization。
  "headers": {
    "Authorization": "Bearer <token>",
    "Content-Type": "application/json"
  },
  // query: Query 参数，列表接口可传分页和筛选条件。
  "query": {},
  // body: Body 请求体，写接口按业务表单提交字段。
  "body": {
    "name": "加班审批积分规则",
    "process_code": "PROC-OVERTIME",
    "hours_field": "加班时长",
    "points_per_hour": "10.00",
    "min_hours": "1.00",
    "max_points_per_order": 0,
    "status": 0,
    "remark": "MVP 默认不自动启用"
  }
}
```

在线调试：

```api-test
{
  "method": "POST",
  "path": "/system/points/rules/create",
  "headers": {
    "Authorization": "Bearer <token>",
    "Content-Type": "application/json"
  },
  "query": {},
  "body": {
    "name": "加班审批积分规则",
    "process_code": "PROC-OVERTIME",
    "hours_field": "加班时长",
    "points_per_hour": "10.00",
    "min_hours": "1.00",
    "max_points_per_order": 0,
    "status": 0,
    "remark": "MVP 默认不自动启用"
  }
}
```
响应示例：

```jsonc
{
  // code: 业务响应码；成功固定为 200，认证、权限、路由和业务异常分别使用 401/403/404/500。
  "code": 200,
  // info: 业务提示文案；字段名固定为 info，不使用 message 作为主字段。
  "info": "新增成功",
  // data: POST /system/points/rules/create响应数据；下方字段为本接口可对接的稳定结构。
  "data": {
    // id: 主键ID。
    "id": 1,
    // tenant_id: 租户ID。
    "tenant_id": 1,
    // name: 规则名称。
    "name": "示例名称",
    // status: 状态(1启用,0禁用)。
    "status": 1,
    // remark: 备注。
    "remark": "示例内容",
    // created_at: 创建时间。
    "created_at": "2026-05-29 10:00:00",
    // updated_at: 更新时间。
    "updated_at": "2026-05-29 10:00:00",
    // created_by: 创建者。
    "created_by": 1,
    // updated_by: 更新者。
    "updated_by": 1,
    // source_type: 积分来源类型。
    "source_type": "示例值",
    // process_code: 审批流程编码。
    "process_code": "示例值",
    // hours_field: 审批时长字段。
    "hours_field": "示例值",
    // points_per_hour: 每小时积分。
    "points_per_hour": "示例值",
    // min_hours: 最小有效小时。
    "min_hours": "示例值",
    // max_points_per_order: 单单最高积分。
    "max_points_per_order": "示例值"
  },
  // path: 当前请求路径；调试时应与实际 Path 保持一致。
  "path": "/system/points/rules/create"
}
```


### 12. PUT /system/points/rules/update/{id}

<!-- API_CASE: PUT /system/points/rules/update/{id} -->

`PUT /system/points/rules/update/{id}`

```jsonc
{
  // method: HTTP 方法，必须与接口表格中的方法一致。
  "method": "PUT",
  // path: 请求路径，占位符按实际 ID 或业务参数替换。
  "path": "/system/points/rules/update/{id}",
  // headers: 请求头，登录或权限接口需要 Authorization。
  "headers": {
    "Authorization": "Bearer <token>",
    "Content-Type": "application/json"
  },
  // query: Query 参数，列表接口可传分页和筛选条件。
  "query": {},
  // body: Body 请求体，写接口按业务表单提交字段。
  "body": {
    "name": "加班审批积分规则",
    "process_code": "PROC-OVERTIME",
    "hours_field": "加班时长",
    "points_per_hour": "10.00",
    "min_hours": "1.00",
    "max_points_per_order": 0,
    "status": 0,
    "remark": "MVP 默认不自动启用"
  }
}
```

在线调试：

```api-test
{
  "method": "PUT",
  "path": "/system/points/rules/update/{id}",
  "headers": {
    "Authorization": "Bearer <token>",
    "Content-Type": "application/json"
  },
  "query": {},
  "body": {
    "name": "加班审批积分规则",
    "process_code": "PROC-OVERTIME",
    "hours_field": "加班时长",
    "points_per_hour": "10.00",
    "min_hours": "1.00",
    "max_points_per_order": 0,
    "status": 0,
    "remark": "MVP 默认不自动启用"
  }
}
```
响应示例：

```jsonc
{
  // code: 业务响应码；成功固定为 200，认证、权限、路由和业务异常分别使用 401/403/404/500。
  "code": 200,
  // info: 业务提示文案；字段名固定为 info，不使用 message 作为主字段。
  "info": "更新成功",
  // data: PUT /system/points/rules/update/{id}响应数据；下方字段为本接口可对接的稳定结构。
  "data": true,
  // path: 当前请求路径；调试时应与实际 Path 保持一致。
  "path": "/system/points/rules/update/{id}"
}
```


### 13. PUT /system/points/rules/status/{id}

<!-- API_CASE: PUT /system/points/rules/status/{id} -->

`PUT /system/points/rules/status/{id}`

```jsonc
{
  // method: HTTP 方法，必须与接口表格中的方法一致。
  "method": "PUT",
  // path: 请求路径，占位符按实际 ID 或业务参数替换。
  "path": "/system/points/rules/status/{id}",
  // headers: 请求头，登录或权限接口需要 Authorization。
  "headers": {
    "Authorization": "Bearer <token>",
    "Content-Type": "application/json"
  },
  // query: Query 参数，列表接口可传分页和筛选条件。
  "query": {},
  // body: Body 请求体，写接口按业务表单提交字段。
  "body": {
    "status": 1
  }
}
```

在线调试：

```api-test
{
  "method": "PUT",
  "path": "/system/points/rules/status/{id}",
  "headers": {
    "Authorization": "Bearer <token>",
    "Content-Type": "application/json"
  },
  "query": {},
  "body": {
    "status": 1
  }
}
```
响应示例：

```jsonc
{
  // code: 业务响应码；成功固定为 200，认证、权限、路由和业务异常分别使用 401/403/404/500。
  "code": 200,
  // info: 业务提示文案；字段名固定为 info，不使用 message 作为主字段。
  "info": "更新成功",
  // data: PUT /system/points/rules/status/{id}响应数据；下方字段为本接口可对接的稳定结构。
  "data": true,
  // path: 当前请求路径；调试时应与实际 Path 保持一致。
  "path": "/system/points/rules/status/{id}"
}
```


### 14. DELETE /system/points/rules/delete/{ids}

<!-- API_CASE: DELETE /system/points/rules/delete/{ids} -->

`DELETE /system/points/rules/delete/{ids}`

```jsonc
{
  // method: HTTP 方法，必须与接口表格中的方法一致。
  "method": "DELETE",
  // path: 请求路径，占位符按实际 ID 或业务参数替换。
  "path": "/system/points/rules/delete/{ids}",
  // headers: 请求头，登录或权限接口需要 Authorization。
  "headers": {
    "Authorization": "Bearer <token>",
    "Content-Type": "application/json"
  },
  // query: Query 参数，列表接口可传分页和筛选条件。
  "query": {},
  // body: Body 请求体，写接口按业务表单提交字段。
  "body": ""
}
```

在线调试：

```api-test
{
  "method": "DELETE",
  "path": "/system/points/rules/delete/{ids}",
  "headers": {
    "Authorization": "Bearer <token>",
    "Content-Type": "application/json"
  },
  "query": {},
  "body": ""
}
```
响应示例：

```jsonc
{
  // code: 业务响应码；成功固定为 200，认证、权限、路由和业务异常分别使用 401/403/404/500。
  "code": 200,
  // info: 业务提示文案；字段名固定为 info，不使用 message 作为主字段。
  "info": "获取成功",
  // data: DELETE /system/points/rules/delete/{ids}响应数据；下方字段为本接口可对接的稳定结构。
  "data": true,
  // path: 当前请求路径；调试时应与实际 Path 保持一致。
  "path": "/system/points/rules/delete/{ids}"
}
```


### 15. GET /system/points/members/index

<!-- API_CASE: GET /system/points/members/index -->

`GET /system/points/members/index`

```jsonc
{
  // method: HTTP 方法，必须与接口表格中的方法一致。
  "method": "GET",
  // path: 请求路径，占位符按实际 ID 或业务参数替换。
  "path": "/system/points/members/index",
  // headers: 请求头，登录或权限接口需要 Authorization。
  "headers": {
    "Authorization": "Bearer <token>",
    "Content-Type": "application/json"
  },
  // query: Query 参数，列表接口可传分页和筛选条件。
  "query": {
    "page": 1,
    "pageSize": 15
  },
  // body: Body 请求体，写接口按业务表单提交字段。
  "body": ""
}
```

在线调试：

```api-test
{
  "method": "GET",
  "path": "/system/points/members/index",
  "headers": {
    "Authorization": "Bearer <token>",
    "Content-Type": "application/json"
  },
  "query": {
    "page": 1,
    "pageSize": 15
  },
  "body": ""
}
```
响应示例：

```jsonc
{
  // code: 业务响应码；成功固定为 200，认证、权限、路由和业务异常分别使用 401/403/404/500。
  "code": 200,
  // info: 业务提示文案；字段名固定为 info，不使用 message 作为主字段。
  "info": "获取成功",
  // data: GET /system/points/members/index响应数据；下方字段为本接口可对接的稳定结构。
  "data": {
    // items: 当前页数据列表；数组元素为本接口对应业务对象。
    "items": [
      {
        // id: 主键ID。
        "id": 1,
        // tenant_id: 租户ID。
        "tenant_id": 1,
        // name: 员工姓名。
        "name": "示例名称",
        // status: 状态(1启用,0禁用)。
        "status": 1,
        // created_by: 创建者。
        "created_by": 1,
        // updated_by: 更新者。
        "updated_by": 1,
        // dingtalk_user_bind_id: 钉钉用户映射ID。
        "dingtalk_user_bind_id": 1,
        // dingtalk_userid: 钉钉用户ID。
        "dingtalk_userid": "示例值",
        // unionid: 钉钉 UnionID。
        "unionid": "demo_code",
        // job_number: 工号。
        "job_number": "示例值",
        // created_at: 创建时间。
        "created_at": "2026-05-29 10:00:00",
        // updated_at: 更新时间。
        "updated_at": "2026-05-29 10:00:00"
      }
    ],
    // pageInfo: 分页信息；标准分页接口由 CoreMapper 返回 total/totalPage/currentPage。
    "pageInfo": {
      // total: 符合当前权限、租户和筛选条件的总记录数。
      "total": 0,
      // totalPage: 按当前 pageSize 计算的总页数。
      "totalPage": 0,
      // currentPage: 当前页码，从 1 开始。
      "currentPage": 1
    }
  },
  // path: 当前请求路径；调试时应与实际 Path 保持一致。
  "path": "/system/points/members/index"
}
```


### 16. GET /system/points/members/info/{id}

<!-- API_CASE: GET /system/points/members/info/{id} -->

`GET /system/points/members/info/{id}`

```jsonc
{
  // method: HTTP 方法，必须与接口表格中的方法一致。
  "method": "GET",
  // path: 请求路径，占位符按实际 ID 或业务参数替换。
  "path": "/system/points/members/info/{id}",
  // headers: 请求头，登录或权限接口需要 Authorization。
  "headers": {
    "Authorization": "Bearer <token>",
    "Content-Type": "application/json"
  },
  // query: Query 参数，列表接口可传分页和筛选条件。
  "query": {},
  // body: Body 请求体，写接口按业务表单提交字段。
  "body": ""
}
```

在线调试：

```api-test
{
  "method": "GET",
  "path": "/system/points/members/info/{id}",
  "headers": {
    "Authorization": "Bearer <token>",
    "Content-Type": "application/json"
  },
  "query": {},
  "body": ""
}
```
响应示例：

```jsonc
{
  // code: 业务响应码；成功固定为 200，认证、权限、路由和业务异常分别使用 401/403/404/500。
  "code": 200,
  // info: 业务提示文案；字段名固定为 info，不使用 message 作为主字段。
  "info": "获取成功",
  // data: GET /system/points/members/info/{id}响应数据；下方字段为本接口可对接的稳定结构。
  "data": {
    // id: 主键ID。
    "id": 1,
    // tenant_id: 租户ID。
    "tenant_id": 1,
    // name: 员工姓名。
    "name": "示例名称",
    // status: 状态(1启用,0禁用)。
    "status": 1,
    // created_by: 创建者。
    "created_by": 1,
    // updated_by: 更新者。
    "updated_by": 1,
    // dingtalk_user_bind_id: 钉钉用户映射ID。
    "dingtalk_user_bind_id": 1,
    // dingtalk_userid: 钉钉用户ID。
    "dingtalk_userid": "示例值",
    // unionid: 钉钉 UnionID。
    "unionid": "demo_code",
    // job_number: 工号。
    "job_number": "示例值",
    // mobile: 手机号。
    "mobile": "13800000000",
    // email: 邮箱。
    "email": "demo@example.com",
    // avatar: 头像。
    "avatar": "https://example.com/demo.png",
    // points_balance: 当前积分余额。
    "points_balance": "示例值",
    // total_earned: 累计获得积分。
    "total_earned": "示例值",
    // total_spent: 累计消耗积分。
    "total_spent": "示例值",
    // created_at: 创建时间。
    "created_at": "2026-05-29 10:00:00",
    // updated_at: 更新时间。
    "updated_at": "2026-05-29 10:00:00"
  },
  // path: 当前请求路径；调试时应与实际 Path 保持一致。
  "path": "/system/points/members/info/{id}"
}
```


### 17. PUT /system/points/members/status/{id}

<!-- API_CASE: PUT /system/points/members/status/{id} -->

`PUT /system/points/members/status/{id}`

```jsonc
{
  // method: HTTP 方法，必须与接口表格中的方法一致。
  "method": "PUT",
  // path: 请求路径，占位符按实际 ID 或业务参数替换。
  "path": "/system/points/members/status/{id}",
  // headers: 请求头，登录或权限接口需要 Authorization。
  "headers": {
    "Authorization": "Bearer <token>",
    "Content-Type": "application/json"
  },
  // query: Query 参数，列表接口可传分页和筛选条件。
  "query": {},
  // body: Body 请求体，写接口按业务表单提交字段。
  "body": {
    "status": 1
  }
}
```

在线调试：

```api-test
{
  "method": "PUT",
  "path": "/system/points/members/status/{id}",
  "headers": {
    "Authorization": "Bearer <token>",
    "Content-Type": "application/json"
  },
  "query": {},
  "body": {
    "status": 1
  }
}
```
响应示例：

```jsonc
{
  // code: 业务响应码；成功固定为 200，认证、权限、路由和业务异常分别使用 401/403/404/500。
  "code": 200,
  // info: 业务提示文案；字段名固定为 info，不使用 message 作为主字段。
  "info": "更新成功",
  // data: PUT /system/points/members/status/{id}响应数据；下方字段为本接口可对接的稳定结构。
  "data": true,
  // path: 当前请求路径；调试时应与实际 Path 保持一致。
  "path": "/system/points/members/status/{id}"
}
```


### 18. POST /system/points/members/adjust/{id}

<!-- API_CASE: POST /system/points/members/adjust/{id} -->

`POST /system/points/members/adjust/{id}`

```jsonc
{
  // method: HTTP 方法，必须与接口表格中的方法一致。
  "method": "POST",
  // path: 请求路径，占位符按实际 ID 或业务参数替换。
  "path": "/system/points/members/adjust/{id}",
  // headers: 请求头，登录或权限接口需要 Authorization。
  "headers": {
    "Authorization": "Bearer <token>",
    "Content-Type": "application/json"
  },
  // query: Query 参数，列表接口可传分页和筛选条件。
  "query": {},
  // body: Body 请求体，写接口按业务表单提交字段。
  "body": {
    "amount": 10,
    "remark": "手工调整"
  }
}
```

在线调试：

```api-test
{
  "method": "POST",
  "path": "/system/points/members/adjust/{id}",
  "headers": {
    "Authorization": "Bearer <token>",
    "Content-Type": "application/json"
  },
  "query": {},
  "body": {
    "amount": 10,
    "remark": "手工调整"
  }
}
```
响应示例：

```jsonc
{
  // code: 业务响应码；成功固定为 200，认证、权限、路由和业务异常分别使用 401/403/404/500。
  "code": 200,
  // info: 业务提示文案；字段名固定为 info，不使用 message 作为主字段。
  "info": "获取成功",
  // data: POST /system/points/members/adjust/{id}响应数据；下方字段为本接口可对接的稳定结构。
  "data": {
    // id: 主键ID。
    "id": 1,
    // tenant_id: 租户ID。
    "tenant_id": 1,
    // name: 员工姓名。
    "name": "示例名称",
    // status: 状态(1启用,0禁用)。
    "status": 1,
    // created_by: 创建者。
    "created_by": 1,
    // updated_by: 更新者。
    "updated_by": 1,
    // dingtalk_user_bind_id: 钉钉用户映射ID。
    "dingtalk_user_bind_id": 1,
    // dingtalk_userid: 钉钉用户ID。
    "dingtalk_userid": "示例值",
    // unionid: 钉钉 UnionID。
    "unionid": "demo_code",
    // job_number: 工号。
    "job_number": "示例值",
    // mobile: 手机号。
    "mobile": "13800000000",
    // email: 邮箱。
    "email": "demo@example.com",
    // avatar: 头像。
    "avatar": "https://example.com/demo.png",
    // points_balance: 当前积分余额。
    "points_balance": "示例值",
    // total_earned: 累计获得积分。
    "total_earned": "示例值",
    // total_spent: 累计消耗积分。
    "total_spent": "示例值",
    // created_at: 创建时间。
    "created_at": "2026-05-29 10:00:00",
    // updated_at: 更新时间。
    "updated_at": "2026-05-29 10:00:00"
  },
  // path: 当前请求路径；调试时应与实际 Path 保持一致。
  "path": "/system/points/members/adjust/{id}"
}
```


### 19. GET /system/points/ledger/index

<!-- API_CASE: GET /system/points/ledger/index -->

`GET /system/points/ledger/index`

```jsonc
{
  // method: HTTP 方法，必须与接口表格中的方法一致。
  "method": "GET",
  // path: 请求路径，占位符按实际 ID 或业务参数替换。
  "path": "/system/points/ledger/index",
  // headers: 请求头，登录或权限接口需要 Authorization。
  "headers": {
    "Authorization": "Bearer <token>",
    "Content-Type": "application/json"
  },
  // query: Query 参数，列表接口可传分页和筛选条件。
  "query": {
    "page": 1,
    "pageSize": 15
  },
  // body: Body 请求体，写接口按业务表单提交字段。
  "body": ""
}
```

在线调试：

```api-test
{
  "method": "GET",
  "path": "/system/points/ledger/index",
  "headers": {
    "Authorization": "Bearer <token>",
    "Content-Type": "application/json"
  },
  "query": {
    "page": 1,
    "pageSize": 15
  },
  "body": ""
}
```
响应示例：

```jsonc
{
  // code: 业务响应码；成功固定为 200，认证、权限、路由和业务异常分别使用 401/403/404/500。
  "code": 200,
  // info: 业务提示文案；字段名固定为 info，不使用 message 作为主字段。
  "info": "获取成功",
  // data: GET /system/points/ledger/index响应数据；下方字段为本接口可对接的稳定结构。
  "data": {
    // items: 当前页数据列表；数组元素为本接口对应业务对象。
    "items": [
      {
        // id: 主键ID。
        "id": 1,
        // tenant_id: 租户ID。
        "tenant_id": 1,
        // type: 流水类型。
        "type": "default",
        // status: 状态(1启用,0禁用)。
        "status": 1,
        // remark: 备注。
        "remark": "示例内容",
        // created_by: 创建者。
        "created_by": 1,
        // updated_by: 更新者。
        "updated_by": 1,
        // member_id: 员工ID。
        "member_id": 1,
        // source_type: 来源类型。
        "source_type": "示例值",
        // source_id: 来源ID。
        "source_id": 1,
        // created_at: 创建时间。
        "created_at": "2026-05-29 10:00:00",
        // updated_at: 更新时间。
        "updated_at": "2026-05-29 10:00:00"
      }
    ],
    // pageInfo: 分页信息；标准分页接口由 CoreMapper 返回 total/totalPage/currentPage。
    "pageInfo": {
      // total: 符合当前权限、租户和筛选条件的总记录数。
      "total": 0,
      // totalPage: 按当前 pageSize 计算的总页数。
      "totalPage": 0,
      // currentPage: 当前页码，从 1 开始。
      "currentPage": 1
    }
  },
  // path: 当前请求路径；调试时应与实际 Path 保持一致。
  "path": "/system/points/ledger/index"
}
```


### 20. GET /system/points/gifts/index

<!-- API_CASE: GET /system/points/gifts/index -->

`GET /system/points/gifts/index`

```jsonc
{
  // method: HTTP 方法，必须与接口表格中的方法一致。
  "method": "GET",
  // path: 请求路径，占位符按实际 ID 或业务参数替换。
  "path": "/system/points/gifts/index",
  // headers: 请求头，登录或权限接口需要 Authorization。
  "headers": {
    "Authorization": "Bearer <token>",
    "Content-Type": "application/json"
  },
  // query: Query 参数，列表接口可传分页和筛选条件。
  "query": {
    "page": 1,
    "pageSize": 15
  },
  // body: Body 请求体，写接口按业务表单提交字段。
  "body": ""
}
```

在线调试：

```api-test
{
  "method": "GET",
  "path": "/system/points/gifts/index",
  "headers": {
    "Authorization": "Bearer <token>",
    "Content-Type": "application/json"
  },
  "query": {
    "page": 1,
    "pageSize": 15
  },
  "body": ""
}
```
响应示例：

```jsonc
{
  // code: 业务响应码；成功固定为 200，认证、权限、路由和业务异常分别使用 401/403/404/500。
  "code": 200,
  // info: 业务提示文案；字段名固定为 info，不使用 message 作为主字段。
  "info": "获取成功",
  // data: GET /system/points/gifts/index响应数据；下方字段为本接口可对接的稳定结构。
  "data": {
    // items: 当前页数据列表；数组元素为本接口对应业务对象。
    "items": [
      {
        // id: 主键ID。
        "id": 1,
        // tenant_id: 租户ID。
        "tenant_id": 1,
        // code: 礼品编码。
        "code": "demo_code",
        // name: 礼品名称。
        "name": "示例名称",
        // status: 状态(1启用,0禁用)。
        "status": 1,
        // sort: 排序。
        "sort": 1,
        // created_by: 创建者。
        "created_by": 1,
        // updated_by: 更新者。
        "updated_by": 1,
        // cover: 封面。
        "cover": "https://example.com/demo.png",
        // description: 礼品说明。
        "description": "示例内容",
        // created_at: 创建时间。
        "created_at": "2026-05-29 10:00:00",
        // updated_at: 更新时间。
        "updated_at": "2026-05-29 10:00:00"
      }
    ],
    // pageInfo: 分页信息；标准分页接口由 CoreMapper 返回 total/totalPage/currentPage。
    "pageInfo": {
      // total: 符合当前权限、租户和筛选条件的总记录数。
      "total": 0,
      // totalPage: 按当前 pageSize 计算的总页数。
      "totalPage": 0,
      // currentPage: 当前页码，从 1 开始。
      "currentPage": 1
    }
  },
  // path: 当前请求路径；调试时应与实际 Path 保持一致。
  "path": "/system/points/gifts/index"
}
```


### 21. GET /system/points/gifts/info/{id}

<!-- API_CASE: GET /system/points/gifts/info/{id} -->

`GET /system/points/gifts/info/{id}`

```jsonc
{
  // method: HTTP 方法，必须与接口表格中的方法一致。
  "method": "GET",
  // path: 请求路径，占位符按实际 ID 或业务参数替换。
  "path": "/system/points/gifts/info/{id}",
  // headers: 请求头，登录或权限接口需要 Authorization。
  "headers": {
    "Authorization": "Bearer <token>",
    "Content-Type": "application/json"
  },
  // query: Query 参数，列表接口可传分页和筛选条件。
  "query": {},
  // body: Body 请求体，写接口按业务表单提交字段。
  "body": ""
}
```

在线调试：

```api-test
{
  "method": "GET",
  "path": "/system/points/gifts/info/{id}",
  "headers": {
    "Authorization": "Bearer <token>",
    "Content-Type": "application/json"
  },
  "query": {},
  "body": ""
}
```
响应示例：

```jsonc
{
  // code: 业务响应码；成功固定为 200，认证、权限、路由和业务异常分别使用 401/403/404/500。
  "code": 200,
  // info: 业务提示文案；字段名固定为 info，不使用 message 作为主字段。
  "info": "获取成功",
  // data: GET /system/points/gifts/info/{id}响应数据；下方字段为本接口可对接的稳定结构。
  "data": {
    // id: 主键ID。
    "id": 1,
    // tenant_id: 租户ID。
    "tenant_id": 1,
    // code: 礼品编码。
    "code": "demo_code",
    // name: 礼品名称。
    "name": "示例名称",
    // status: 状态(1启用,0禁用)。
    "status": 1,
    // sort: 排序。
    "sort": 1,
    // created_at: 创建时间。
    "created_at": "2026-05-29 10:00:00",
    // updated_at: 更新时间。
    "updated_at": "2026-05-29 10:00:00",
    // created_by: 创建者。
    "created_by": 1,
    // updated_by: 更新者。
    "updated_by": 1,
    // cover: 封面。
    "cover": "https://example.com/demo.png",
    // description: 礼品说明。
    "description": "示例内容",
    // points_price: 兑换积分。
    "points_price": "示例值",
    // stock_total: 总库存。
    "stock_total": "示例值",
    // stock_available: 可用库存。
    "stock_available": "示例值",
    // exchanged_count: 已兑换数量。
    "exchanged_count": 1
  },
  // path: 当前请求路径；调试时应与实际 Path 保持一致。
  "path": "/system/points/gifts/info/{id}"
}
```


### 22. POST /system/points/gifts/create

<!-- API_CASE: POST /system/points/gifts/create -->

`POST /system/points/gifts/create`

```jsonc
{
  // method: HTTP 方法，必须与接口表格中的方法一致。
  "method": "POST",
  // path: 请求路径，占位符按实际 ID 或业务参数替换。
  "path": "/system/points/gifts/create",
  // headers: 请求头，登录或权限接口需要 Authorization。
  "headers": {
    "Authorization": "Bearer <token>",
    "Content-Type": "application/json"
  },
  // query: Query 参数，列表接口可传分页和筛选条件。
  "query": {},
  // body: Body 请求体，写接口按业务表单提交字段。
  "body": {
    "name": "咖啡券",
    "code": "GIFT-COFFEE",
    "cover": "https://example.com/coffee.png",
    "description": "企业内部礼品",
    "points_price": 100,
    "stock_total": 10,
    "stock_available": 10,
    "status": 0
  }
}
```

在线调试：

```api-test
{
  "method": "POST",
  "path": "/system/points/gifts/create",
  "headers": {
    "Authorization": "Bearer <token>",
    "Content-Type": "application/json"
  },
  "query": {},
  "body": {
    "name": "咖啡券",
    "code": "GIFT-COFFEE",
    "cover": "https://example.com/coffee.png",
    "description": "企业内部礼品",
    "points_price": 100,
    "stock_total": 10,
    "stock_available": 10,
    "status": 0
  }
}
```
响应示例：

```jsonc
{
  // code: 业务响应码；成功固定为 200，认证、权限、路由和业务异常分别使用 401/403/404/500。
  "code": 200,
  // info: 业务提示文案；字段名固定为 info，不使用 message 作为主字段。
  "info": "新增成功",
  // data: POST /system/points/gifts/create响应数据；下方字段为本接口可对接的稳定结构。
  "data": {
    // id: 主键ID。
    "id": 1,
    // tenant_id: 租户ID。
    "tenant_id": 1,
    // code: 礼品编码。
    "code": "demo_code",
    // name: 礼品名称。
    "name": "示例名称",
    // status: 状态(1启用,0禁用)。
    "status": 1,
    // sort: 排序。
    "sort": 1,
    // created_at: 创建时间。
    "created_at": "2026-05-29 10:00:00",
    // updated_at: 更新时间。
    "updated_at": "2026-05-29 10:00:00",
    // created_by: 创建者。
    "created_by": 1,
    // updated_by: 更新者。
    "updated_by": 1,
    // cover: 封面。
    "cover": "https://example.com/demo.png",
    // description: 礼品说明。
    "description": "示例内容",
    // points_price: 兑换积分。
    "points_price": "示例值",
    // stock_total: 总库存。
    "stock_total": "示例值",
    // stock_available: 可用库存。
    "stock_available": "示例值",
    // exchanged_count: 已兑换数量。
    "exchanged_count": 1
  },
  // path: 当前请求路径；调试时应与实际 Path 保持一致。
  "path": "/system/points/gifts/create"
}
```


### 23. PUT /system/points/gifts/update/{id}

<!-- API_CASE: PUT /system/points/gifts/update/{id} -->

`PUT /system/points/gifts/update/{id}`

```jsonc
{
  // method: HTTP 方法，必须与接口表格中的方法一致。
  "method": "PUT",
  // path: 请求路径，占位符按实际 ID 或业务参数替换。
  "path": "/system/points/gifts/update/{id}",
  // headers: 请求头，登录或权限接口需要 Authorization。
  "headers": {
    "Authorization": "Bearer <token>",
    "Content-Type": "application/json"
  },
  // query: Query 参数，列表接口可传分页和筛选条件。
  "query": {},
  // body: Body 请求体，写接口按业务表单提交字段。
  "body": {
    "name": "咖啡券",
    "code": "GIFT-COFFEE",
    "cover": "https://example.com/coffee.png",
    "description": "企业内部礼品",
    "points_price": 100,
    "stock_total": 10,
    "stock_available": 10,
    "status": 0
  }
}
```

在线调试：

```api-test
{
  "method": "PUT",
  "path": "/system/points/gifts/update/{id}",
  "headers": {
    "Authorization": "Bearer <token>",
    "Content-Type": "application/json"
  },
  "query": {},
  "body": {
    "name": "咖啡券",
    "code": "GIFT-COFFEE",
    "cover": "https://example.com/coffee.png",
    "description": "企业内部礼品",
    "points_price": 100,
    "stock_total": 10,
    "stock_available": 10,
    "status": 0
  }
}
```
响应示例：

```jsonc
{
  // code: 业务响应码；成功固定为 200，认证、权限、路由和业务异常分别使用 401/403/404/500。
  "code": 200,
  // info: 业务提示文案；字段名固定为 info，不使用 message 作为主字段。
  "info": "更新成功",
  // data: PUT /system/points/gifts/update/{id}响应数据；下方字段为本接口可对接的稳定结构。
  "data": true,
  // path: 当前请求路径；调试时应与实际 Path 保持一致。
  "path": "/system/points/gifts/update/{id}"
}
```


### 24. PUT /system/points/gifts/status/{id}

<!-- API_CASE: PUT /system/points/gifts/status/{id} -->

`PUT /system/points/gifts/status/{id}`

```jsonc
{
  // method: HTTP 方法，必须与接口表格中的方法一致。
  "method": "PUT",
  // path: 请求路径，占位符按实际 ID 或业务参数替换。
  "path": "/system/points/gifts/status/{id}",
  // headers: 请求头，登录或权限接口需要 Authorization。
  "headers": {
    "Authorization": "Bearer <token>",
    "Content-Type": "application/json"
  },
  // query: Query 参数，列表接口可传分页和筛选条件。
  "query": {},
  // body: Body 请求体，写接口按业务表单提交字段。
  "body": {
    "status": 1
  }
}
```

在线调试：

```api-test
{
  "method": "PUT",
  "path": "/system/points/gifts/status/{id}",
  "headers": {
    "Authorization": "Bearer <token>",
    "Content-Type": "application/json"
  },
  "query": {},
  "body": {
    "status": 1
  }
}
```
响应示例：

```jsonc
{
  // code: 业务响应码；成功固定为 200，认证、权限、路由和业务异常分别使用 401/403/404/500。
  "code": 200,
  // info: 业务提示文案；字段名固定为 info，不使用 message 作为主字段。
  "info": "更新成功",
  // data: PUT /system/points/gifts/status/{id}响应数据；下方字段为本接口可对接的稳定结构。
  "data": true,
  // path: 当前请求路径；调试时应与实际 Path 保持一致。
  "path": "/system/points/gifts/status/{id}"
}
```


### 25. POST /system/points/gifts/stock/{id}

<!-- API_CASE: POST /system/points/gifts/stock/{id} -->

`POST /system/points/gifts/stock/{id}`

```jsonc
{
  // method: HTTP 方法，必须与接口表格中的方法一致。
  "method": "POST",
  // path: 请求路径，占位符按实际 ID 或业务参数替换。
  "path": "/system/points/gifts/stock/{id}",
  // headers: 请求头，登录或权限接口需要 Authorization。
  "headers": {
    "Authorization": "Bearer <token>",
    "Content-Type": "application/json"
  },
  // query: Query 参数，列表接口可传分页和筛选条件。
  "query": {},
  // body: Body 请求体，写接口按业务表单提交字段。
  "body": {
    "delta": 10
  }
}
```

在线调试：

```api-test
{
  "method": "POST",
  "path": "/system/points/gifts/stock/{id}",
  "headers": {
    "Authorization": "Bearer <token>",
    "Content-Type": "application/json"
  },
  "query": {},
  "body": {
    "delta": 10
  }
}
```
响应示例：

```jsonc
{
  // code: 业务响应码；成功固定为 200，认证、权限、路由和业务异常分别使用 401/403/404/500。
  "code": 200,
  // info: 业务提示文案；字段名固定为 info，不使用 message 作为主字段。
  "info": "获取成功",
  // data: POST /system/points/gifts/stock/{id}响应数据；下方字段为本接口可对接的稳定结构。
  "data": {
    // id: 主键ID。
    "id": 1,
    // tenant_id: 租户ID。
    "tenant_id": 1,
    // code: 礼品编码。
    "code": "demo_code",
    // name: 礼品名称。
    "name": "示例名称",
    // status: 状态(1启用,0禁用)。
    "status": 1,
    // sort: 排序。
    "sort": 1,
    // created_at: 创建时间。
    "created_at": "2026-05-29 10:00:00",
    // updated_at: 更新时间。
    "updated_at": "2026-05-29 10:00:00",
    // created_by: 创建者。
    "created_by": 1,
    // updated_by: 更新者。
    "updated_by": 1,
    // cover: 封面。
    "cover": "https://example.com/demo.png",
    // description: 礼品说明。
    "description": "示例内容",
    // points_price: 兑换积分。
    "points_price": "示例值",
    // stock_total: 总库存。
    "stock_total": "示例值",
    // stock_available: 可用库存。
    "stock_available": "示例值",
    // exchanged_count: 已兑换数量。
    "exchanged_count": 1
  },
  // path: 当前请求路径；调试时应与实际 Path 保持一致。
  "path": "/system/points/gifts/stock/{id}"
}
```


### 26. DELETE /system/points/gifts/delete/{ids}

<!-- API_CASE: DELETE /system/points/gifts/delete/{ids} -->

`DELETE /system/points/gifts/delete/{ids}`

```jsonc
{
  // method: HTTP 方法，必须与接口表格中的方法一致。
  "method": "DELETE",
  // path: 请求路径，占位符按实际 ID 或业务参数替换。
  "path": "/system/points/gifts/delete/{ids}",
  // headers: 请求头，登录或权限接口需要 Authorization。
  "headers": {
    "Authorization": "Bearer <token>",
    "Content-Type": "application/json"
  },
  // query: Query 参数，列表接口可传分页和筛选条件。
  "query": {},
  // body: Body 请求体，写接口按业务表单提交字段。
  "body": ""
}
```

在线调试：

```api-test
{
  "method": "DELETE",
  "path": "/system/points/gifts/delete/{ids}",
  "headers": {
    "Authorization": "Bearer <token>",
    "Content-Type": "application/json"
  },
  "query": {},
  "body": ""
}
```
响应示例：

```jsonc
{
  // code: 业务响应码；成功固定为 200，认证、权限、路由和业务异常分别使用 401/403/404/500。
  "code": 200,
  // info: 业务提示文案；字段名固定为 info，不使用 message 作为主字段。
  "info": "获取成功",
  // data: DELETE /system/points/gifts/delete/{ids}响应数据；下方字段为本接口可对接的稳定结构。
  "data": true,
  // path: 当前请求路径；调试时应与实际 Path 保持一致。
  "path": "/system/points/gifts/delete/{ids}"
}
```


### 27. GET /system/points/exchanges/index

<!-- API_CASE: GET /system/points/exchanges/index -->

`GET /system/points/exchanges/index`

```jsonc
{
  // method: HTTP 方法，必须与接口表格中的方法一致。
  "method": "GET",
  // path: 请求路径，占位符按实际 ID 或业务参数替换。
  "path": "/system/points/exchanges/index",
  // headers: 请求头，登录或权限接口需要 Authorization。
  "headers": {
    "Authorization": "Bearer <token>",
    "Content-Type": "application/json"
  },
  // query: Query 参数，列表接口可传分页和筛选条件。
  "query": {
    "page": 1,
    "pageSize": 15
  },
  // body: Body 请求体，写接口按业务表单提交字段。
  "body": ""
}
```

在线调试：

```api-test
{
  "method": "GET",
  "path": "/system/points/exchanges/index",
  "headers": {
    "Authorization": "Bearer <token>",
    "Content-Type": "application/json"
  },
  "query": {
    "page": 1,
    "pageSize": 15
  },
  "body": ""
}
```
响应示例：

```jsonc
{
  // code: 业务响应码；成功固定为 200，认证、权限、路由和业务异常分别使用 401/403/404/500。
  "code": 200,
  // info: 业务提示文案；字段名固定为 info，不使用 message 作为主字段。
  "info": "获取成功",
  // data: GET /system/points/exchanges/index响应数据；下方字段为本接口可对接的稳定结构。
  "data": {
    // items: 当前页数据列表；数组元素为本接口对应业务对象。
    "items": [
      {
        // id: 主键ID。
        "id": 1,
        // tenant_id: 租户ID。
        "tenant_id": 1,
        // status: 状态(1启用,0禁用)。
        "status": 1,
        // remark: 员工备注。
        "remark": "示例内容",
        // created_by: 创建者。
        "created_by": 1,
        // updated_by: 更新者。
        "updated_by": 1,
        // exchange_no: 兑换单号。
        "exchange_no": "示例值",
        // member_id: 员工ID。
        "member_id": 1,
        // gift_id: 礼品ID。
        "gift_id": 1,
        // gift_name: 礼品名称快照。
        "gift_name": "示例值",
        // created_at: 创建时间。
        "created_at": "2026-05-29 10:00:00",
        // updated_at: 更新时间。
        "updated_at": "2026-05-29 10:00:00"
      }
    ],
    // pageInfo: 分页信息；标准分页接口由 CoreMapper 返回 total/totalPage/currentPage。
    "pageInfo": {
      // total: 符合当前权限、租户和筛选条件的总记录数。
      "total": 0,
      // totalPage: 按当前 pageSize 计算的总页数。
      "totalPage": 0,
      // currentPage: 当前页码，从 1 开始。
      "currentPage": 1
    }
  },
  // path: 当前请求路径；调试时应与实际 Path 保持一致。
  "path": "/system/points/exchanges/index"
}
```


### 28. GET /system/points/exchanges/info/{id}

<!-- API_CASE: GET /system/points/exchanges/info/{id} -->

`GET /system/points/exchanges/info/{id}`

```jsonc
{
  // method: HTTP 方法，必须与接口表格中的方法一致。
  "method": "GET",
  // path: 请求路径，占位符按实际 ID 或业务参数替换。
  "path": "/system/points/exchanges/info/{id}",
  // headers: 请求头，登录或权限接口需要 Authorization。
  "headers": {
    "Authorization": "Bearer <token>",
    "Content-Type": "application/json"
  },
  // query: Query 参数，列表接口可传分页和筛选条件。
  "query": {},
  // body: Body 请求体，写接口按业务表单提交字段。
  "body": ""
}
```

在线调试：

```api-test
{
  "method": "GET",
  "path": "/system/points/exchanges/info/{id}",
  "headers": {
    "Authorization": "Bearer <token>",
    "Content-Type": "application/json"
  },
  "query": {},
  "body": ""
}
```
响应示例：

```jsonc
{
  // code: 业务响应码；成功固定为 200，认证、权限、路由和业务异常分别使用 401/403/404/500。
  "code": 200,
  // info: 业务提示文案；字段名固定为 info，不使用 message 作为主字段。
  "info": "获取成功",
  // data: GET /system/points/exchanges/info/{id}响应数据；下方字段为本接口可对接的稳定结构。
  "data": {
    // id: 主键ID。
    "id": 1,
    // tenant_id: 租户ID。
    "tenant_id": 1,
    // status: 状态(1启用,0禁用)。
    "status": 1,
    // remark: 员工备注。
    "remark": "示例内容",
    // created_by: 创建者。
    "created_by": 1,
    // updated_by: 更新者。
    "updated_by": 1,
    // exchange_no: 兑换单号。
    "exchange_no": "示例值",
    // member_id: 员工ID。
    "member_id": 1,
    // gift_id: 礼品ID。
    "gift_id": 1,
    // gift_name: 礼品名称快照。
    "gift_name": "示例值",
    // points_price: 单价积分快照。
    "points_price": "示例值",
    // quantity: 数量。
    "quantity": "示例值",
    // total_points: 总积分。
    "total_points": "示例值",
    // exchange_status: 兑换状态。
    "exchange_status": "示例值",
    // contact_name: 联系人。
    "contact_name": "示例名称",
    // contact_mobile: 联系电话。
    "contact_mobile": "示例值",
    // created_at: 创建时间。
    "created_at": "2026-05-29 10:00:00",
    // updated_at: 更新时间。
    "updated_at": "2026-05-29 10:00:00"
  },
  // path: 当前请求路径；调试时应与实际 Path 保持一致。
  "path": "/system/points/exchanges/info/{id}"
}
```


### 29. POST /system/points/exchanges/approve/{id}

<!-- API_CASE: POST /system/points/exchanges/approve/{id} -->

`POST /system/points/exchanges/approve/{id}`

```jsonc
{
  // method: HTTP 方法，必须与接口表格中的方法一致。
  "method": "POST",
  // path: 请求路径，占位符按实际 ID 或业务参数替换。
  "path": "/system/points/exchanges/approve/{id}",
  // headers: 请求头，登录或权限接口需要 Authorization。
  "headers": {
    "Authorization": "Bearer <token>",
    "Content-Type": "application/json"
  },
  // query: Query 参数，列表接口可传分页和筛选条件。
  "query": {},
  // body: Body 请求体，写接口按业务表单提交字段。
  "body": {
    "remark": "后台处理"
  }
}
```

在线调试：

```api-test
{
  "method": "POST",
  "path": "/system/points/exchanges/approve/{id}",
  "headers": {
    "Authorization": "Bearer <token>",
    "Content-Type": "application/json"
  },
  "query": {},
  "body": {
    "remark": "后台处理"
  }
}
```
响应示例：

```jsonc
{
  // code: 业务响应码；成功固定为 200，认证、权限、路由和业务异常分别使用 401/403/404/500。
  "code": 200,
  // info: 业务提示文案；字段名固定为 info，不使用 message 作为主字段。
  "info": "获取成功",
  // data: POST /system/points/exchanges/approve/{id}响应数据；下方字段为本接口可对接的稳定结构。
  "data": {
    // id: 主键ID。
    "id": 1,
    // tenant_id: 租户ID。
    "tenant_id": 1,
    // status: 状态(1启用,0禁用)。
    "status": 1,
    // remark: 员工备注。
    "remark": "示例内容",
    // created_by: 创建者。
    "created_by": 1,
    // updated_by: 更新者。
    "updated_by": 1,
    // exchange_no: 兑换单号。
    "exchange_no": "示例值",
    // member_id: 员工ID。
    "member_id": 1,
    // gift_id: 礼品ID。
    "gift_id": 1,
    // gift_name: 礼品名称快照。
    "gift_name": "示例值",
    // points_price: 单价积分快照。
    "points_price": "示例值",
    // quantity: 数量。
    "quantity": "示例值",
    // total_points: 总积分。
    "total_points": "示例值",
    // exchange_status: 兑换状态。
    "exchange_status": "示例值",
    // contact_name: 联系人。
    "contact_name": "示例名称",
    // contact_mobile: 联系电话。
    "contact_mobile": "示例值",
    // created_at: 创建时间。
    "created_at": "2026-05-29 10:00:00",
    // updated_at: 更新时间。
    "updated_at": "2026-05-29 10:00:00"
  },
  // path: 当前请求路径；调试时应与实际 Path 保持一致。
  "path": "/system/points/exchanges/approve/{id}"
}
```


### 30. POST /system/points/exchanges/reject/{id}

<!-- API_CASE: POST /system/points/exchanges/reject/{id} -->

`POST /system/points/exchanges/reject/{id}`

```jsonc
{
  // method: HTTP 方法，必须与接口表格中的方法一致。
  "method": "POST",
  // path: 请求路径，占位符按实际 ID 或业务参数替换。
  "path": "/system/points/exchanges/reject/{id}",
  // headers: 请求头，登录或权限接口需要 Authorization。
  "headers": {
    "Authorization": "Bearer <token>",
    "Content-Type": "application/json"
  },
  // query: Query 参数，列表接口可传分页和筛选条件。
  "query": {},
  // body: Body 请求体，写接口按业务表单提交字段。
  "body": {
    "remark": "后台处理"
  }
}
```

在线调试：

```api-test
{
  "method": "POST",
  "path": "/system/points/exchanges/reject/{id}",
  "headers": {
    "Authorization": "Bearer <token>",
    "Content-Type": "application/json"
  },
  "query": {},
  "body": {
    "remark": "后台处理"
  }
}
```
响应示例：

```jsonc
{
  // code: 业务响应码；成功固定为 200，认证、权限、路由和业务异常分别使用 401/403/404/500。
  "code": 200,
  // info: 业务提示文案；字段名固定为 info，不使用 message 作为主字段。
  "info": "获取成功",
  // data: POST /system/points/exchanges/reject/{id}响应数据；下方字段为本接口可对接的稳定结构。
  "data": {
    // id: 主键ID。
    "id": 1,
    // tenant_id: 租户ID。
    "tenant_id": 1,
    // status: 状态(1启用,0禁用)。
    "status": 1,
    // remark: 员工备注。
    "remark": "示例内容",
    // created_by: 创建者。
    "created_by": 1,
    // updated_by: 更新者。
    "updated_by": 1,
    // exchange_no: 兑换单号。
    "exchange_no": "示例值",
    // member_id: 员工ID。
    "member_id": 1,
    // gift_id: 礼品ID。
    "gift_id": 1,
    // gift_name: 礼品名称快照。
    "gift_name": "示例值",
    // points_price: 单价积分快照。
    "points_price": "示例值",
    // quantity: 数量。
    "quantity": "示例值",
    // total_points: 总积分。
    "total_points": "示例值",
    // exchange_status: 兑换状态。
    "exchange_status": "示例值",
    // contact_name: 联系人。
    "contact_name": "示例名称",
    // contact_mobile: 联系电话。
    "contact_mobile": "示例值",
    // created_at: 创建时间。
    "created_at": "2026-05-29 10:00:00",
    // updated_at: 更新时间。
    "updated_at": "2026-05-29 10:00:00"
  },
  // path: 当前请求路径；调试时应与实际 Path 保持一致。
  "path": "/system/points/exchanges/reject/{id}"
}
```


### 31. POST /system/points/exchanges/deliver/{id}

<!-- API_CASE: POST /system/points/exchanges/deliver/{id} -->

`POST /system/points/exchanges/deliver/{id}`

```jsonc
{
  // method: HTTP 方法，必须与接口表格中的方法一致。
  "method": "POST",
  // path: 请求路径，占位符按实际 ID 或业务参数替换。
  "path": "/system/points/exchanges/deliver/{id}",
  // headers: 请求头，登录或权限接口需要 Authorization。
  "headers": {
    "Authorization": "Bearer <token>",
    "Content-Type": "application/json"
  },
  // query: Query 参数，列表接口可传分页和筛选条件。
  "query": {},
  // body: Body 请求体，写接口按业务表单提交字段。
  "body": {
    "remark": "后台处理"
  }
}
```

在线调试：

```api-test
{
  "method": "POST",
  "path": "/system/points/exchanges/deliver/{id}",
  "headers": {
    "Authorization": "Bearer <token>",
    "Content-Type": "application/json"
  },
  "query": {},
  "body": {
    "remark": "后台处理"
  }
}
```
响应示例：

```jsonc
{
  // code: 业务响应码；成功固定为 200，认证、权限、路由和业务异常分别使用 401/403/404/500。
  "code": 200,
  // info: 业务提示文案；字段名固定为 info，不使用 message 作为主字段。
  "info": "获取成功",
  // data: POST /system/points/exchanges/deliver/{id}响应数据；下方字段为本接口可对接的稳定结构。
  "data": {
    // id: 主键ID。
    "id": 1,
    // tenant_id: 租户ID。
    "tenant_id": 1,
    // status: 状态(1启用,0禁用)。
    "status": 1,
    // remark: 员工备注。
    "remark": "示例内容",
    // created_by: 创建者。
    "created_by": 1,
    // updated_by: 更新者。
    "updated_by": 1,
    // exchange_no: 兑换单号。
    "exchange_no": "示例值",
    // member_id: 员工ID。
    "member_id": 1,
    // gift_id: 礼品ID。
    "gift_id": 1,
    // gift_name: 礼品名称快照。
    "gift_name": "示例值",
    // points_price: 单价积分快照。
    "points_price": "示例值",
    // quantity: 数量。
    "quantity": "示例值",
    // total_points: 总积分。
    "total_points": "示例值",
    // exchange_status: 兑换状态。
    "exchange_status": "示例值",
    // contact_name: 联系人。
    "contact_name": "示例名称",
    // contact_mobile: 联系电话。
    "contact_mobile": "示例值",
    // created_at: 创建时间。
    "created_at": "2026-05-29 10:00:00",
    // updated_at: 更新时间。
    "updated_at": "2026-05-29 10:00:00"
  },
  // path: 当前请求路径；调试时应与实际 Path 保持一致。
  "path": "/system/points/exchanges/deliver/{id}"
}
```


### 32. GET /system/points/reports/overview

<!-- API_CASE: GET /system/points/reports/overview -->

`GET /system/points/reports/overview`

```jsonc
{
  // method: HTTP 方法，必须与接口表格中的方法一致。
  "method": "GET",
  // path: 请求路径，占位符按实际 ID 或业务参数替换。
  "path": "/system/points/reports/overview",
  // headers: 请求头，登录或权限接口需要 Authorization。
  "headers": {
    "Authorization": "Bearer <token>",
    "Content-Type": "application/json"
  },
  // query: Query 参数，列表接口可传分页和筛选条件。
  "query": {},
  // body: Body 请求体，写接口按业务表单提交字段。
  "body": ""
}
```

在线调试：

```api-test
{
  "method": "GET",
  "path": "/system/points/reports/overview",
  "headers": {
    "Authorization": "Bearer <token>",
    "Content-Type": "application/json"
  },
  "query": {},
  "body": ""
}
```
响应示例：

```jsonc
{
  // code: 业务响应码；成功固定为 200，认证、权限、路由和业务异常分别使用 401/403/404/500。
  "code": 200,
  // info: 业务提示文案；字段名固定为 info，不使用 message 作为主字段。
  "info": "获取成功",
  // data: GET /system/points/reports/overview响应数据；下方字段为本接口可对接的稳定结构。
  "data": {
    // id: 配置或业务对象 ID。
    "id": 1,
    // name: 配置或业务对象名称。
    "name": "示例名称",
    // status: 状态值。
    "status": 1
  },
  // path: 当前请求路径；调试时应与实际 Path 保持一致。
  "path": "/system/points/reports/overview"
}
```


### 33. GET /points/account/auth/dingtalk-config

<!-- API_CASE: GET /points/account/auth/dingtalk-config -->

`GET /points/account/auth/dingtalk-config`

```jsonc
{
  // method: HTTP 方法，必须与接口表格中的方法一致。
  "method": "GET",
  // path: 请求路径，占位符按实际 ID 或业务参数替换。
  "path": "/points/account/auth/dingtalk-config",
  // headers: 请求头，登录或权限接口需要 Authorization。
  "headers": {
    "Authorization": "Bearer <token>",
    "Content-Type": "application/json"
  },
  // query: Query 参数，列表接口可传分页和筛选条件。
  "query": {
    "config_id": 1
  },
  // body: Body 请求体，写接口按业务表单提交字段。
  "body": ""
}
```

在线调试：

```api-test
{
  "method": "GET",
  "path": "/points/account/auth/dingtalk-config",
  "headers": {
    "Authorization": "Bearer <token>",
    "Content-Type": "application/json"
  },
  "query": {
    "config_id": 1
  },
  "body": ""
}
```
响应示例：

```jsonc
{
  // code: 业务响应码；成功固定为 200，认证、权限、路由和业务异常分别使用 401/403/404/500。
  "code": 200,
  // info: 业务提示文案；字段名固定为 info，不使用 message 作为主字段。
  "info": "获取成功",
  // data: GET /points/account/auth/dingtalk-config响应数据；下方字段为本接口可对接的稳定结构。
  "data": {
    // id: 主键ID。
    "id": 1,
    // tenant_id: 租户ID。
    "tenant_id": 1,
    // name: 配置名称。
    "name": "示例名称",
    // status: 状态(1启用,0禁用)。
    "status": 1,
    // created_by: 创建者。
    "created_by": 1,
    // updated_by: 更新者。
    "updated_by": 1,
    // corp_id: 钉钉 CorpId（组织 ID）。
    "corp_id": 1,
    // agent_id: 钉钉原企业内部应用 AgentId。
    "agent_id": 1,
    // points_base_url: 积分员工端访问地址。
    "points_base_url": "https://example.com/demo.png",
    // root_dept_id: 同步根部门ID。
    "root_dept_id": 1,
    // approval_process_code: 加班审批流程编码。
    "approval_process_code": "示例值",
    // approval_hours_field: 审批单加班时长字段。
    "approval_hours_field": "示例值",
    // approval_user_field: 审批单员工字段。
    "approval_user_field": "示例值",
    // callback_token: 钉钉回调校验 Token 密文。
    "callback_token": "示例值",
    // sync_enabled: 是否允许同步。
    "sync_enabled": "示例值",
    // notify_enabled: 是否允许发送通知。
    "notify_enabled": "示例值",
    // created_at: 创建时间。
    "created_at": "2026-05-29 10:00:00",
    // updated_at: 更新时间。
    "updated_at": "2026-05-29 10:00:00"
  },
  // path: 当前请求路径；调试时应与实际 Path 保持一致。
  "path": "/points/account/auth/dingtalk-config"
}
```


### 34. POST /points/account/auth/dingtalk-login

<!-- API_CASE: POST /points/account/auth/dingtalk-login -->

`POST /points/account/auth/dingtalk-login`

```jsonc
{
  // method: HTTP 方法，必须与接口表格中的方法一致。
  "method": "POST",
  // path: 请求路径，占位符按实际 ID 或业务参数替换。
  "path": "/points/account/auth/dingtalk-login",
  // headers: 请求头，登录或权限接口需要 Authorization。
  "headers": {
    "Authorization": "Bearer <token>",
    "Content-Type": "application/json"
  },
  // query: Query 参数，列表接口可传分页和筛选条件。
  "query": {},
  // body: Body 请求体，写接口按业务表单提交字段。
  "body": {
    "config_id": 1,
    "code": "ding-auth-code"
  }
}
```

在线调试：

```api-test
{
  "method": "POST",
  "path": "/points/account/auth/dingtalk-login",
  "headers": {
    "Authorization": "Bearer <token>",
    "Content-Type": "application/json"
  },
  "query": {},
  "body": {
    "config_id": 1,
    "code": "ding-auth-code"
  }
}
```
响应示例：

```jsonc
{
  // code: 业务响应码；成功固定为 200，认证、权限、路由和业务异常分别使用 401/403/404/500。
  "code": 200,
  // info: 业务提示文案；字段名固定为 info，不使用 message 作为主字段。
  "info": "获取成功",
  // data: POST /points/account/auth/dingtalk-login响应数据；下方字段为本接口可对接的稳定结构。
  "data": {
    // token: 登录成功后的访问令牌，后续请求放入 Authorization Bearer。
    "token": "<token>",
    // user: 当前登录用户基础资料。
    "user": {
      // id: 主键ID。
      "id": 1,
      // tenant_id: 租户ID。
      "tenant_id": 1,
      // username: 用户名。
      "username": "示例名称",
      // nickname: 用户昵称。
      "nickname": "示例名称",
      // status: 状态(1启用,0禁用)。
      "status": 1,
      // remark: 备注。
      "remark": "示例内容",
      // phone: 手机号码。
      "phone": "13800000000",
      // created_at: 创建时间。
      "created_at": "2026-05-29 10:00:00",
      // updated_at: 更新时间。
      "updated_at": "2026-05-29 10:00:00",
      // deleted_at: 删除时间。
      "deleted_at": null
    },
    // auth_user_model: 当前认证用户模型。
    "auth_user_model": "System\\Model\\SystemUser"
  },
  // path: 当前请求路径；调试时应与实际 Path 保持一致。
  "path": "/points/account/auth/dingtalk-login"
}
```


### 35. POST /points/account/auth/refresh

<!-- API_CASE: POST /points/account/auth/refresh -->

`POST /points/account/auth/refresh`

```jsonc
{
  // method: HTTP 方法，必须与接口表格中的方法一致。
  "method": "POST",
  // path: 请求路径，占位符按实际 ID 或业务参数替换。
  "path": "/points/account/auth/refresh",
  // headers: 请求头，登录或权限接口需要 Authorization。
  "headers": {
    "Authorization": "Bearer <token>",
    "Content-Type": "application/json"
  },
  // query: Query 参数，列表接口可传分页和筛选条件。
  "query": {},
  // body: Body 请求体，写接口按业务表单提交字段。
  "body": {}
}
```

在线调试：

```api-test
{
  "method": "POST",
  "path": "/points/account/auth/refresh",
  "headers": {
    "Authorization": "Bearer <token>",
    "Content-Type": "application/json"
  },
  "query": {},
  "body": {}
}
```
响应示例：

```jsonc
{
  // code: 业务响应码；成功固定为 200，认证、权限、路由和业务异常分别使用 401/403/404/500。
  "code": 200,
  // info: 业务提示文案；字段名固定为 info，不使用 message 作为主字段。
  "info": "获取成功",
  // data: POST /points/account/auth/refresh响应数据；下方字段为本接口可对接的稳定结构。
  "data": "<token>",
  // path: 当前请求路径；调试时应与实际 Path 保持一致。
  "path": "/points/account/auth/refresh"
}
```


### 36. POST /points/account/auth/profile

<!-- API_CASE: POST /points/account/auth/profile -->

`POST /points/account/auth/profile`

```jsonc
{
  // method: HTTP 方法，必须与接口表格中的方法一致。
  "method": "POST",
  // path: 请求路径，占位符按实际 ID 或业务参数替换。
  "path": "/points/account/auth/profile",
  // headers: 请求头，登录或权限接口需要 Authorization。
  "headers": {
    "Authorization": "Bearer <token>",
    "Content-Type": "application/json"
  },
  // query: Query 参数，列表接口可传分页和筛选条件。
  "query": {},
  // body: Body 请求体，写接口按业务表单提交字段。
  "body": {}
}
```

在线调试：

```api-test
{
  "method": "POST",
  "path": "/points/account/auth/profile",
  "headers": {
    "Authorization": "Bearer <token>",
    "Content-Type": "application/json"
  },
  "query": {},
  "body": {}
}
```
响应示例：

```jsonc
{
  // code: 业务响应码；成功固定为 200，认证、权限、路由和业务异常分别使用 401/403/404/500。
  "code": 200,
  // info: 业务提示文案；字段名固定为 info，不使用 message 作为主字段。
  "info": "获取成功",
  // data: POST /points/account/auth/profile响应数据；下方字段为本接口可对接的稳定结构。
  "data": {
    // userId: 当前登录用户 ID，字符串格式便于前端路由和权限组件使用。
    "userId": "1",
    // username: 当前登录用户名。
    "username": "admin",
    // realName: 当前登录人展示名称。
    "realName": "系统管理员",
    // avatar: 头像地址。
    "avatar": "https://example.com/avatar.png",
    // roles: 当前登录人角色名称列表。
    "roles": [
      "admin"
    ],
    // homePath: 登录后默认首页路径。
    "homePath": "/dashboard",
    // token: 当前访问令牌；刷新资料时随请求头同步返回。
    "token": "<token>",
    // profile: 当前用户完整资料对象。
    "profile": {
      // id: 主键ID。
      "id": 1,
      // tenant_id: 租户ID。
      "tenant_id": 1,
      // username: 用户名。
      "username": "示例名称",
      // nickname: 用户昵称。
      "nickname": "示例名称",
      // status: 状态(1启用,0禁用)。
      "status": 1,
      // remark: 备注。
      "remark": "示例内容",
      // phone: 手机号码。
      "phone": "13800000000",
      // created_at: 创建时间。
      "created_at": "2026-05-29 10:00:00",
      // updated_at: 更新时间。
      "updated_at": "2026-05-29 10:00:00",
      // deleted_at: 删除时间。
      "deleted_at": null
    },
    // auth_user_model: 当前认证用户模型。
    "auth_user_model": "Asset\\Model\\AssetEmployee"
  },
  // path: 当前请求路径；调试时应与实际 Path 保持一致。
  "path": "/points/account/auth/profile"
}
```


### 37. GET /points/account/auth/codes

<!-- API_CASE: GET /points/account/auth/codes -->

`GET /points/account/auth/codes`

```jsonc
{
  // method: HTTP 方法，必须与接口表格中的方法一致。
  "method": "GET",
  // path: 请求路径，占位符按实际 ID 或业务参数替换。
  "path": "/points/account/auth/codes",
  // headers: 请求头，登录或权限接口需要 Authorization。
  "headers": {
    "Authorization": "Bearer <token>",
    "Content-Type": "application/json"
  },
  // query: Query 参数，列表接口可传分页和筛选条件。
  "query": {},
  // body: Body 请求体，写接口按业务表单提交字段。
  "body": ""
}
```

在线调试：

```api-test
{
  "method": "GET",
  "path": "/points/account/auth/codes",
  "headers": {
    "Authorization": "Bearer <token>",
    "Content-Type": "application/json"
  },
  "query": {},
  "body": ""
}
```
响应示例：

```jsonc
{
  // code: 业务响应码；成功固定为 200，认证、权限、路由和业务异常分别使用 401/403/404/500。
  "code": 200,
  // info: 业务提示文案；字段名固定为 info，不使用 message 作为主字段。
  "info": "获取成功",
  // data: GET /points/account/auth/codes响应数据；下方字段为本接口可对接的稳定结构。
  "data": [
    "system.user.index"
  ],
  // path: 当前请求路径；调试时应与实际 Path 保持一致。
  "path": "/points/account/auth/codes"
}
```


### 38. POST /points/account/auth/logout

<!-- API_CASE: POST /points/account/auth/logout -->

`POST /points/account/auth/logout`

```jsonc
{
  // method: HTTP 方法，必须与接口表格中的方法一致。
  "method": "POST",
  // path: 请求路径，占位符按实际 ID 或业务参数替换。
  "path": "/points/account/auth/logout",
  // headers: 请求头，登录或权限接口需要 Authorization。
  "headers": {
    "Authorization": "Bearer <token>",
    "Content-Type": "application/json"
  },
  // query: Query 参数，列表接口可传分页和筛选条件。
  "query": {},
  // body: Body 请求体，写接口按业务表单提交字段。
  "body": {}
}
```

在线调试：

```api-test
{
  "method": "POST",
  "path": "/points/account/auth/logout",
  "headers": {
    "Authorization": "Bearer <token>",
    "Content-Type": "application/json"
  },
  "query": {},
  "body": {}
}
```
响应示例：

```jsonc
{
  // code: 业务响应码；成功固定为 200，认证、权限、路由和业务异常分别使用 401/403/404/500。
  "code": 200,
  // info: 业务提示文案；字段名固定为 info，不使用 message 作为主字段。
  "info": "登出成功",
  // data: POST /points/account/auth/logout响应数据；下方字段为本接口可对接的稳定结构。
  "data": [],
  // path: 当前请求路径；调试时应与实际 Path 保持一致。
  "path": "/points/account/auth/logout"
}
```


### 39. GET /points/me/balance

<!-- API_CASE: GET /points/me/balance -->

`GET /points/me/balance`

```jsonc
{
  // method: HTTP 方法，必须与接口表格中的方法一致。
  "method": "GET",
  // path: 请求路径，占位符按实际 ID 或业务参数替换。
  "path": "/points/me/balance",
  // headers: 请求头，登录或权限接口需要 Authorization。
  "headers": {
    "Authorization": "Bearer <token>",
    "Content-Type": "application/json"
  },
  // query: Query 参数，列表接口可传分页和筛选条件。
  "query": {},
  // body: Body 请求体，写接口按业务表单提交字段。
  "body": ""
}
```

在线调试：

```api-test
{
  "method": "GET",
  "path": "/points/me/balance",
  "headers": {
    "Authorization": "Bearer <token>",
    "Content-Type": "application/json"
  },
  "query": {},
  "body": ""
}
```
响应示例：

```jsonc
{
  // code: 业务响应码；成功固定为 200，认证、权限、路由和业务异常分别使用 401/403/404/500。
  "code": 200,
  // info: 业务提示文案；字段名固定为 info，不使用 message 作为主字段。
  "info": "获取成功",
  // data: GET /points/me/balance响应数据；下方字段为本接口可对接的稳定结构。
  "data": {
    // id: 配置或业务对象 ID。
    "id": 1,
    // name: 配置或业务对象名称。
    "name": "示例名称",
    // status: 状态值。
    "status": 1
  },
  // path: 当前请求路径；调试时应与实际 Path 保持一致。
  "path": "/points/me/balance"
}
```


### 40. GET /points/me/ledger

<!-- API_CASE: GET /points/me/ledger -->

`GET /points/me/ledger`

```jsonc
{
  // method: HTTP 方法，必须与接口表格中的方法一致。
  "method": "GET",
  // path: 请求路径，占位符按实际 ID 或业务参数替换。
  "path": "/points/me/ledger",
  // headers: 请求头，登录或权限接口需要 Authorization。
  "headers": {
    "Authorization": "Bearer <token>",
    "Content-Type": "application/json"
  },
  // query: Query 参数，列表接口可传分页和筛选条件。
  "query": {
    "page": 1,
    "pageSize": 15
  },
  // body: Body 请求体，写接口按业务表单提交字段。
  "body": ""
}
```

在线调试：

```api-test
{
  "method": "GET",
  "path": "/points/me/ledger",
  "headers": {
    "Authorization": "Bearer <token>",
    "Content-Type": "application/json"
  },
  "query": {
    "page": 1,
    "pageSize": 15
  },
  "body": ""
}
```
响应示例：

```jsonc
{
  // code: 业务响应码；成功固定为 200，认证、权限、路由和业务异常分别使用 401/403/404/500。
  "code": 200,
  // info: 业务提示文案；字段名固定为 info，不使用 message 作为主字段。
  "info": "获取成功",
  // data: GET /points/me/ledger响应数据；下方字段为本接口可对接的稳定结构。
  "data": {
    // id: 主键ID。
    "id": 1,
    // tenant_id: 租户ID。
    "tenant_id": 1,
    // type: 流水类型。
    "type": "default",
    // status: 状态(1启用,0禁用)。
    "status": 1,
    // remark: 备注。
    "remark": "示例内容",
    // created_by: 创建者。
    "created_by": 1,
    // updated_by: 更新者。
    "updated_by": 1,
    // member_id: 员工ID。
    "member_id": 1,
    // source_type: 来源类型。
    "source_type": "示例值",
    // source_id: 来源ID。
    "source_id": 1,
    // direction: 方向(in,out)。
    "direction": "示例值",
    // amount: 积分数量。
    "amount": "示例值",
    // before_balance: 变动前余额。
    "before_balance": "示例值",
    // after_balance: 变动后余额。
    "after_balance": "示例值",
    // operator_type: 操作者类型。
    "operator_type": "示例值",
    // operator_id: 操作者ID。
    "operator_id": 1,
    // created_at: 创建时间。
    "created_at": "2026-05-29 10:00:00",
    // updated_at: 更新时间。
    "updated_at": "2026-05-29 10:00:00"
  },
  // path: 当前请求路径；调试时应与实际 Path 保持一致。
  "path": "/points/me/ledger"
}
```


### 41. GET /points/gifts/index

<!-- API_CASE: GET /points/gifts/index -->

`GET /points/gifts/index`

```jsonc
{
  // method: HTTP 方法，必须与接口表格中的方法一致。
  "method": "GET",
  // path: 请求路径，占位符按实际 ID 或业务参数替换。
  "path": "/points/gifts/index",
  // headers: 请求头，登录或权限接口需要 Authorization。
  "headers": {
    "Authorization": "Bearer <token>",
    "Content-Type": "application/json"
  },
  // query: Query 参数，列表接口可传分页和筛选条件。
  "query": {
    "page": 1,
    "pageSize": 15
  },
  // body: Body 请求体，写接口按业务表单提交字段。
  "body": ""
}
```

在线调试：

```api-test
{
  "method": "GET",
  "path": "/points/gifts/index",
  "headers": {
    "Authorization": "Bearer <token>",
    "Content-Type": "application/json"
  },
  "query": {
    "page": 1,
    "pageSize": 15
  },
  "body": ""
}
```
响应示例：

```jsonc
{
  // code: 业务响应码；成功固定为 200，认证、权限、路由和业务异常分别使用 401/403/404/500。
  "code": 200,
  // info: 业务提示文案；字段名固定为 info，不使用 message 作为主字段。
  "info": "获取成功",
  // data: GET /points/gifts/index响应数据；下方字段为本接口可对接的稳定结构。
  "data": {
    // items: 当前页数据列表；数组元素为本接口对应业务对象。
    "items": [
      {
        // id: 主键ID。
        "id": 1,
        // tenant_id: 租户ID。
        "tenant_id": 1,
        // code: 礼品编码。
        "code": "demo_code",
        // name: 礼品名称。
        "name": "示例名称",
        // status: 状态(1启用,0禁用)。
        "status": 1,
        // sort: 排序。
        "sort": 1,
        // created_by: 创建者。
        "created_by": 1,
        // updated_by: 更新者。
        "updated_by": 1,
        // cover: 封面。
        "cover": "https://example.com/demo.png",
        // description: 礼品说明。
        "description": "示例内容",
        // created_at: 创建时间。
        "created_at": "2026-05-29 10:00:00",
        // updated_at: 更新时间。
        "updated_at": "2026-05-29 10:00:00"
      }
    ],
    // pageInfo: 分页信息；标准分页接口由 CoreMapper 返回 total/totalPage/currentPage。
    "pageInfo": {
      // total: 符合当前权限、租户和筛选条件的总记录数。
      "total": 0,
      // totalPage: 按当前 pageSize 计算的总页数。
      "totalPage": 0,
      // currentPage: 当前页码，从 1 开始。
      "currentPage": 1
    }
  },
  // path: 当前请求路径；调试时应与实际 Path 保持一致。
  "path": "/points/gifts/index"
}
```


### 42. GET /points/gifts/info/{id}

<!-- API_CASE: GET /points/gifts/info/{id} -->

`GET /points/gifts/info/{id}`

```jsonc
{
  // method: HTTP 方法，必须与接口表格中的方法一致。
  "method": "GET",
  // path: 请求路径，占位符按实际 ID 或业务参数替换。
  "path": "/points/gifts/info/{id}",
  // headers: 请求头，登录或权限接口需要 Authorization。
  "headers": {
    "Authorization": "Bearer <token>",
    "Content-Type": "application/json"
  },
  // query: Query 参数，列表接口可传分页和筛选条件。
  "query": {},
  // body: Body 请求体，写接口按业务表单提交字段。
  "body": ""
}
```

在线调试：

```api-test
{
  "method": "GET",
  "path": "/points/gifts/info/{id}",
  "headers": {
    "Authorization": "Bearer <token>",
    "Content-Type": "application/json"
  },
  "query": {},
  "body": ""
}
```
响应示例：

```jsonc
{
  // code: 业务响应码；成功固定为 200，认证、权限、路由和业务异常分别使用 401/403/404/500。
  "code": 200,
  // info: 业务提示文案；字段名固定为 info，不使用 message 作为主字段。
  "info": "获取成功",
  // data: GET /points/gifts/info/{id}响应数据；下方字段为本接口可对接的稳定结构。
  "data": {
    // id: 主键ID。
    "id": 1,
    // tenant_id: 租户ID。
    "tenant_id": 1,
    // code: 礼品编码。
    "code": "demo_code",
    // name: 礼品名称。
    "name": "示例名称",
    // status: 状态(1启用,0禁用)。
    "status": 1,
    // sort: 排序。
    "sort": 1,
    // created_at: 创建时间。
    "created_at": "2026-05-29 10:00:00",
    // updated_at: 更新时间。
    "updated_at": "2026-05-29 10:00:00",
    // created_by: 创建者。
    "created_by": 1,
    // updated_by: 更新者。
    "updated_by": 1,
    // cover: 封面。
    "cover": "https://example.com/demo.png",
    // description: 礼品说明。
    "description": "示例内容",
    // points_price: 兑换积分。
    "points_price": "示例值",
    // stock_total: 总库存。
    "stock_total": "示例值",
    // stock_available: 可用库存。
    "stock_available": "示例值",
    // exchanged_count: 已兑换数量。
    "exchanged_count": 1
  },
  // path: 当前请求路径；调试时应与实际 Path 保持一致。
  "path": "/points/gifts/info/{id}"
}
```


### 43. GET /points/exchanges/index

<!-- API_CASE: GET /points/exchanges/index -->

`GET /points/exchanges/index`

```jsonc
{
  // method: HTTP 方法，必须与接口表格中的方法一致。
  "method": "GET",
  // path: 请求路径，占位符按实际 ID 或业务参数替换。
  "path": "/points/exchanges/index",
  // headers: 请求头，登录或权限接口需要 Authorization。
  "headers": {
    "Authorization": "Bearer <token>",
    "Content-Type": "application/json"
  },
  // query: Query 参数，列表接口可传分页和筛选条件。
  "query": {
    "page": 1,
    "pageSize": 15
  },
  // body: Body 请求体，写接口按业务表单提交字段。
  "body": ""
}
```

在线调试：

```api-test
{
  "method": "GET",
  "path": "/points/exchanges/index",
  "headers": {
    "Authorization": "Bearer <token>",
    "Content-Type": "application/json"
  },
  "query": {
    "page": 1,
    "pageSize": 15
  },
  "body": ""
}
```
响应示例：

```jsonc
{
  // code: 业务响应码；成功固定为 200，认证、权限、路由和业务异常分别使用 401/403/404/500。
  "code": 200,
  // info: 业务提示文案；字段名固定为 info，不使用 message 作为主字段。
  "info": "获取成功",
  // data: GET /points/exchanges/index响应数据；下方字段为本接口可对接的稳定结构。
  "data": {
    // items: 当前页数据列表；数组元素为本接口对应业务对象。
    "items": [
      {
        // id: 主键ID。
        "id": 1,
        // tenant_id: 租户ID。
        "tenant_id": 1,
        // status: 状态(1启用,0禁用)。
        "status": 1,
        // remark: 员工备注。
        "remark": "示例内容",
        // created_by: 创建者。
        "created_by": 1,
        // updated_by: 更新者。
        "updated_by": 1,
        // exchange_no: 兑换单号。
        "exchange_no": "示例值",
        // member_id: 员工ID。
        "member_id": 1,
        // gift_id: 礼品ID。
        "gift_id": 1,
        // gift_name: 礼品名称快照。
        "gift_name": "示例值",
        // created_at: 创建时间。
        "created_at": "2026-05-29 10:00:00",
        // updated_at: 更新时间。
        "updated_at": "2026-05-29 10:00:00"
      }
    ],
    // pageInfo: 分页信息；标准分页接口由 CoreMapper 返回 total/totalPage/currentPage。
    "pageInfo": {
      // total: 符合当前权限、租户和筛选条件的总记录数。
      "total": 0,
      // totalPage: 按当前 pageSize 计算的总页数。
      "totalPage": 0,
      // currentPage: 当前页码，从 1 开始。
      "currentPage": 1
    }
  },
  // path: 当前请求路径；调试时应与实际 Path 保持一致。
  "path": "/points/exchanges/index"
}
```


### 44. POST /points/exchanges/create

<!-- API_CASE: POST /points/exchanges/create -->

`POST /points/exchanges/create`

```jsonc
{
  // method: HTTP 方法，必须与接口表格中的方法一致。
  "method": "POST",
  // path: 请求路径，占位符按实际 ID 或业务参数替换。
  "path": "/points/exchanges/create",
  // headers: 请求头，登录或权限接口需要 Authorization。
  "headers": {
    "Authorization": "Bearer <token>",
    "Content-Type": "application/json"
  },
  // query: Query 参数，列表接口可传分页和筛选条件。
  "query": {},
  // body: Body 请求体，写接口按业务表单提交字段。
  "body": {
    "gift_id": 1,
    "quantity": 1,
    "contact_name": "张三",
    "contact_mobile": "13800000000",
    "address": "前台领取",
    "remark": ""
  }
}
```

在线调试：

```api-test
{
  "method": "POST",
  "path": "/points/exchanges/create",
  "headers": {
    "Authorization": "Bearer <token>",
    "Content-Type": "application/json"
  },
  "query": {},
  "body": {
    "gift_id": 1,
    "quantity": 1,
    "contact_name": "张三",
    "contact_mobile": "13800000000",
    "address": "前台领取",
    "remark": ""
  }
}
```
响应示例：

```jsonc
{
  // code: 业务响应码；成功固定为 200，认证、权限、路由和业务异常分别使用 401/403/404/500。
  "code": 200,
  // info: 业务提示文案；字段名固定为 info，不使用 message 作为主字段。
  "info": "新增成功",
  // data: POST /points/exchanges/create响应数据；下方字段为本接口可对接的稳定结构。
  "data": {
    // id: 主键ID。
    "id": 1,
    // tenant_id: 租户ID。
    "tenant_id": 1,
    // status: 状态(1启用,0禁用)。
    "status": 1,
    // remark: 员工备注。
    "remark": "示例内容",
    // created_by: 创建者。
    "created_by": 1,
    // updated_by: 更新者。
    "updated_by": 1,
    // exchange_no: 兑换单号。
    "exchange_no": "示例值",
    // member_id: 员工ID。
    "member_id": 1,
    // gift_id: 礼品ID。
    "gift_id": 1,
    // gift_name: 礼品名称快照。
    "gift_name": "示例值",
    // points_price: 单价积分快照。
    "points_price": "示例值",
    // quantity: 数量。
    "quantity": "示例值",
    // total_points: 总积分。
    "total_points": "示例值",
    // exchange_status: 兑换状态。
    "exchange_status": "示例值",
    // contact_name: 联系人。
    "contact_name": "示例名称",
    // contact_mobile: 联系电话。
    "contact_mobile": "示例值",
    // created_at: 创建时间。
    "created_at": "2026-05-29 10:00:00",
    // updated_at: 更新时间。
    "updated_at": "2026-05-29 10:00:00"
  },
  // path: 当前请求路径；调试时应与实际 Path 保持一致。
  "path": "/points/exchanges/create"
}
```


### 45. POST /points/exchanges/cancel/{id}

<!-- API_CASE: POST /points/exchanges/cancel/{id} -->

`POST /points/exchanges/cancel/{id}`

```jsonc
{
  // method: HTTP 方法，必须与接口表格中的方法一致。
  "method": "POST",
  // path: 请求路径，占位符按实际 ID 或业务参数替换。
  "path": "/points/exchanges/cancel/{id}",
  // headers: 请求头，登录或权限接口需要 Authorization。
  "headers": {
    "Authorization": "Bearer <token>",
    "Content-Type": "application/json"
  },
  // query: Query 参数，列表接口可传分页和筛选条件。
  "query": {},
  // body: Body 请求体，写接口按业务表单提交字段。
  "body": {
    "remark": "员工取消兑换"
  }
}
```

在线调试：

```api-test
{
  "method": "POST",
  "path": "/points/exchanges/cancel/{id}",
  "headers": {
    "Authorization": "Bearer <token>",
    "Content-Type": "application/json"
  },
  "query": {},
  "body": {
    "remark": "员工取消兑换"
  }
}
```
响应示例：

```jsonc
{
  // code: 业务响应码；成功固定为 200，认证、权限、路由和业务异常分别使用 401/403/404/500。
  "code": 200,
  // info: 业务提示文案；字段名固定为 info，不使用 message 作为主字段。
  "info": "获取成功",
  // data: POST /points/exchanges/cancel/{id}响应数据；下方字段为本接口可对接的稳定结构。
  "data": {
    // id: 主键ID。
    "id": 1,
    // tenant_id: 租户ID。
    "tenant_id": 1,
    // status: 状态(1启用,0禁用)。
    "status": 1,
    // remark: 员工备注。
    "remark": "示例内容",
    // created_by: 创建者。
    "created_by": 1,
    // updated_by: 更新者。
    "updated_by": 1,
    // exchange_no: 兑换单号。
    "exchange_no": "示例值",
    // member_id: 员工ID。
    "member_id": 1,
    // gift_id: 礼品ID。
    "gift_id": 1,
    // gift_name: 礼品名称快照。
    "gift_name": "示例值",
    // points_price: 单价积分快照。
    "points_price": "示例值",
    // quantity: 数量。
    "quantity": "示例值",
    // total_points: 总积分。
    "total_points": "示例值",
    // exchange_status: 兑换状态。
    "exchange_status": "示例值",
    // contact_name: 联系人。
    "contact_name": "示例名称",
    // contact_mobile: 联系电话。
    "contact_mobile": "示例值",
    // created_at: 创建时间。
    "created_at": "2026-05-29 10:00:00",
    // updated_at: 更新时间。
    "updated_at": "2026-05-29 10:00:00"
  },
  // path: 当前请求路径；调试时应与实际 Path 保持一致。
  "path": "/points/exchanges/cancel/{id}"
}
```


### 46. POST /points/dingtalk/approval-callback

<!-- API_CASE: POST /points/dingtalk/approval-callback -->

`POST /points/dingtalk/approval-callback`

```jsonc
{
  // method: HTTP 方法，必须与接口表格中的方法一致。
  "method": "POST",
  // path: 请求路径，占位符按实际 ID 或业务参数替换。
  "path": "/points/dingtalk/approval-callback",
  // headers: 请求头，登录或权限接口需要 Authorization。
  "headers": {
    "Authorization": "Bearer <token>",
    "Content-Type": "application/json"
  },
  // query: Query 参数，列表接口可传分页和筛选条件。
  "query": {},
  // body: Body 请求体，写接口按业务表单提交字段。
  "body": {
    "config_id": 1,
    "token": "callback-token",
    "process_instance_id": "PROC-INST-001"
  }
}
```

在线调试：

```api-test
{
  "method": "POST",
  "path": "/points/dingtalk/approval-callback",
  "headers": {
    "Authorization": "Bearer <token>",
    "Content-Type": "application/json"
  },
  "query": {},
  "body": {
    "config_id": 1,
    "token": "callback-token",
    "process_instance_id": "PROC-INST-001"
  }
}
```
响应示例：

```jsonc
{
  // code: 业务响应码；成功固定为 200，认证、权限、路由和业务异常分别使用 401/403/404/500。
  "code": 200,
  // info: 业务提示文案；字段名固定为 info，不使用 message 作为主字段。
  "info": "获取成功",
  // data: POST /points/dingtalk/approval-callback响应数据；下方字段为本接口可对接的稳定结构。
  "data": {
    // id: 新建记录 ID。
    "id": 1,
    // config_id: config id。
    "config_id": 1,
    // token: 访问令牌。
    "token": "callback-token",
    // process_instance_id: process instance id。
    "process_instance_id": "PROC-INST-001"
  },
  // path: 当前请求路径；调试时应与实际 Path 保持一致。
  "path": "/points/dingtalk/approval-callback"
}
```


<!-- API_CASES_END -->

## 关键边界

- 加班积分只处理钉钉审批实例，不读取钉钉考勤自动计算加班。
- 积分余额只能通过积分流水变动；发放、扣减、退款都在事务内更新余额快照。
- 兑换提交时扣积分和占库存；驳回或取消退回；发放后不可撤销。
- 钉钉同步只维护 Points 自有员工快照，不自动创建 System 用户。
